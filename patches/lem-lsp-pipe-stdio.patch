--- a/extensions/lsp-mode/client.lisp
+++ b/extensions/lsp-mode/client.lisp
@@ -48,2 +48,10 @@
 (defmethod dispose ((client stdio-client))
-  (async-process:delete-process (stdio-client-process client)))
+  (let ((process (stdio-client-process client)))
+    (when (ignore-errors (uiop:process-alive-p process))
+      (ignore-errors (uiop:terminate-process process :urgent t)))
+    (ignore-errors (uiop:wait-process process))
+    (dolist (stream (list (uiop:process-info-input process)
+                          (uiop:process-info-output process)
+                          (uiop:process-info-error-output process)))
+      (when (streamp stream)
+        (ignore-errors (close stream))))))
@@ -51,2 +59,3 @@
 (defmethod alive-p ((client stdio-client))
-  (async-process:process-alive-p (stdio-client-process client)))
+  (ignore-errors
+    (uiop:process-alive-p (stdio-client-process client))))
--- a/extensions/lsp-mode/lem-stdio-transport.lisp
+++ b/extensions/lsp-mode/lem-stdio-transport.lisp
@@ -6,5 +6,3 @@
   (:import-from :jsonrpc/request-response
                 :parse-message)
-  (:import-from :lem-lsp-mode/async-process-stream
-                :make-input-stream)
   (:export :lem-stdio-transport))
@@ -22,5 +20,6 @@
   (let ((instance (call-next-method)))
     (unless (lem-stdio-transport-stream instance)
       (setf (lem-stdio-transport-stream instance)
-            (make-input-stream (lem-stdio-transport-process instance))))
+            (uiop:process-info-output
+             (lem-stdio-transport-process instance))))
     instance))
@@ -53,3 +52,5 @@
-    (async-process:process-send-input
-     (lem-stdio-transport-process transport)
-     body)))
+    (let ((stream
+            (uiop:process-info-input
+             (lem-stdio-transport-process transport))))
+      (write-string body stream)
+      (finish-output stream))))
--- a/extensions/lsp-mode/lsp-mode.lisp
+++ b/extensions/lsp-mode/lsp-mode.lisp
@@ -87,7 +87,15 @@
 (defmethod run-server-using-mode ((mode (eql :stdio)) spec &key directory)
   (let ((command (get-spec-command spec)))
     (check-exist-program (first command) spec)
-    (let ((process (async-process:create-process command
-                                                 :directory directory
-                                                 :nonblock nil)))
+    (let ((process
+            (uiop:launch-program
+             (append '("bash" "-c"
+                       "exec \"$@\" 2>>\"${LEM_YATH_LSP_STDERR:-/dev/null}\""
+                       "lem-lsp-stdio")
+                     command)
+             :directory directory
+             :input :stream
+             :output :stream
+             :error-output :interactive
+             :external-format :utf-8)))
       (make-instance 'client:stdio-client :process process))))
@@ -742,4 +750,12 @@
+(defun workspace/configuration (params)
+  (let ((params (convert-from-json params 'lsp:configuration-params)))
+    (map 'vector
+         (lambda (item)
+           (declare (ignore item))
+           +null+)
+         (lsp:configuration-params-items params))))
+
 (defun register-lsp-method (workspace method function)
   (jsonrpc:expose (lem-language-client/client:client-connection (workspace-client workspace))
                   method
                   function))
@@ -755,7 +771,10 @@
   (register-lsp-method workspace
                        "window/logMessage"
                        'window/log-message)
+  (register-lsp-method workspace
+                       "workspace/configuration"
+                       'workspace/configuration)
   (initialize workspace
               (lambda ()
                 (funcall continuation workspace))
               :error error))
--- a/extensions/lsp-mode/client-capabilities.json
+++ b/extensions/lsp-mode/client-capabilities.json
@@ -1,4 +1,6 @@
 {
-  "workspace": {},
+  "workspace": {
+    "configuration": true
+  },
   "general": {
     "positionEncodings": [
