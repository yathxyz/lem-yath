diff --git a/extensions/legit/porcelain-git.lisp b/extensions/legit/porcelain-git.lisp
index 3e1555d..5062788 100644
--- a/extensions/legit/porcelain-git.lisp
+++ b/extensions/legit/porcelain-git.lisp
@@ -302,10 +302,7 @@
 ;; Save our script as a string at compile time.
 (defparameter *rebase-script-content*
-  #+(or lem-ncurses lem-sdl2)
   (str:from-file
    (asdf:system-relative-pathname (asdf:find-system "lem-legit")
                                   "scripts/dumbrebaseeditor.sh"))
-  #-(or lem-ncurses lem-sdl2)
-  ""
   "Our dumb editor shell script, saved as a string at compile time.
   We then save it to the user's ~/.lem/legit/rebaseetidor.sh at first use.")
@@ -318,4 +315,3 @@
         (ensure-directories-exist legit-path)
-        (unless (uiop:file-exists-p script-path)
-          (str:to-file script-path *rebase-script-content*))
+        (str:to-file script-path *rebase-script-content*)
         ;; Ensure the file is executable.
@@ -359,5 +355,10 @@
-  (let ((editor (uiop:getenv "EDITOR")))
-    (setf (uiop:getenv "EDITOR") (rebase-script-path))
+  ;; GIT_EDITOR and core.editor take precedence over EDITOR.  Use Git's
+  ;; dedicated sequence-editor override so a configured reusable editor does
+  ;; not recursively open the todo while this process is waiting for it.
+  (let ((sequence-editor (uiop:getenv "GIT_SEQUENCE_EDITOR")))
+    (setf (uiop:getenv "GIT_SEQUENCE_EDITOR")
+          (format nil "bash ~a"
+                  (uiop:escape-shell-token (rebase-script-path))))
     (unwind-protect
          ;; xxx: get the error output, if any, to get explanations of failure.
          (let ((process (uiop:launch-program (list
@@ -386 +387,3 @@
-      (setf (uiop:getenv "EDITOR") editor))))
+      (if sequence-editor
+          (setf (uiop:getenv "GIT_SEQUENCE_EDITOR") sequence-editor)
+          (uiop:symbol-call :sb-posix :unsetenv "GIT_SEQUENCE_EDITOR")))))
