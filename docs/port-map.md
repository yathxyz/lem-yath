# Port map: every declared Emacs package → its Lem disposition

This is a package-level orientation only. The authoritative status and test
evidence live in `docs/parity-ledger.tsv`; a package row here can summarize
several independently exact, approximate, or missing behaviors.

Status legend:
- **lem-builtin** — feature ships in the Lem image; configured/enabled by the port
- **ported** — reimplemented in Common Lisp in this repo (`lem-yath/src/...`)
- **n/a** — Emacs-plumbing with no meaning in Lem (or unused in the Emacs config itself)
- **partial** — core workflow ported, listed aspects missing
- **gap** — no Lem counterpart; not faithfully portable in scope (reason given)

| Emacs package | Status | Lem equivalent / location |
|---|---|---|
| evil | lem-builtin+ported/partial | `lem-vi-mode`, enabled in `src/vi.lisp`; `src/cursor-state.lisp` supplies the configured cursors and buffer-local Emacs state, including GNU Emacs undo on `C-/`, `C-_`, and `C-x u`. `SPC y v` activates the patched conditional screen/logical policy for `j/k`, `gj/gk`, `0/g0`, `$/g$`, `I/A`, `D/C`, line operators/registers, paste, and `V`, restoring ordinary logical-line behavior when disabled. Rendering and virtual motions share Emacs-style space/tab word boundaries with exact-width fallback for long tokens (`patches/lem-vi-screen-line.patch`, `patches/lem-word-boundary-wrap.patch`, `scripts/screen-line-test.sh`). |
| evil-collection | lem-builtin | vi-mode's own mode integrations |
| evil-surround | ported/partial | standard `ys`/`ds`/`cs`, visual `S`, row-wise Visual Block surrounds, common delimiter padding, tags, and ordinary/prefix call prompts. Tag input uses Lem's editable prompt with the configured Emacs motion/editing keys, `C-g` rollback, Return attribute retention, and immediate explicit-`>` submission. Character deletion/change chooses the narrowest balanced enclosure, keeps code/string/comment/Lisp-fence delimiters in safe domains, ignores escapes and syntax decoys, preflights both edits, and fails closed on malformed or multi-character block-string delimiters. Mode-local custom pair functions and multi-character block-string editing remain gaps (`src/vi.lisp`, `scripts/surround-test.sh`) |
| evil-snipe | ported | configured 2.1.3 behavior: visible `s/S/f/F/t/T`, whole-visible `;`/`,` and transient pair repeats, exact inclusive/exclusive operators, counts/dot/jumplist behavior, leading-whitespace skipping, and incremental/final faces (`src/vi.lisp`, `scripts/snipe-test.sh`) |
| evil-nerd-commenter | ported | `g c` operator (`src/vi.lisp`) |
| evil-org | ported/partial | native `.org` buffers provide mode-local folding, visible-line motion, GNU-style `gh/gl/gk/gj/gH` element-tree navigation across the bounded parser, Org-aware `o/O`, the configured `0/$/I/A` endpoint and insertion policy, context-dispatched Meta editing, and all eight active `ae/ie`, `aE/iE`, `ar/ir`, `aR/iR` operator/Visual text objects. Always-active `(`/`)` motions match the pinned double-space sentence model, including wrapped prose and Org table field boundaries with GNU count behavior; `{`/`}` traverse headings, prose, flat and complex lists, formula-owning tables, affiliated keywords, drawers, block paragraphs, and clock groups. All three motion families preserve counts, Normal/Visual behavior, and Evil's context-sensitive characterwise/linewise operator shape; empty elements and malformed blocks remain no-ops. Normal `d` retains Evil motions, counts, registers, undo, and `ds`, while repairing safe nested ordered-list segments (including `[@N]` cookies) and terminal-profile headline-tag alignment; unsupported continuation, tab-structured, or mixed-marker repairs abort before mutation. Normal one-character `x/X` preserves Org table width, whereas counted and Visual deletion and operator-pending Snipe retain their stock behavior. Normal, doubled, counted, and Visual `<`/`>` ranges context-dispatch like pinned Evil-Org: selected headings change one level, safe list-item ranges reindent with ordered-list repair, the first top-level list item triggers the literal one-column whole-list rule, a same-line table range moves by its selected cell boundaries while short counted motions still move once, doubled table operators and ordinary text shift four columns, and every edit remains one undo step. Formula-owning tables repair local numeric `$N`/`@N` references across row/column insertion, deletion, and movement, exclude `remote(...)`, remove deleted direct formulas, respect blank-line non-association, and undo atomically with the table; range-endpoint deletion refuses byte-identically before it could invalidate the target. Top-level heading promotion and unsafe list ranges abort before mutation. The separate boundary model covers conservative inline objects/cells, bracket/plain links, balanced citations/references, depth-three subscripts/superscripts, same-line dollar/delimited LaTeX, bounded multiline double-dollar/delimited LaTeX, and flat TeX macros/entities, paragraphs/rows/flat blocks, point-sensitive items/lists, formula-owning tables, matched property/LOGBOOK/generic drawers with child elements and greater/subtree ancestry, sections/headlines, count anchoring/ancestry, owned post-blank, exact char/line shapes, reverse Visual ranges, and class-specific fail-closed unsafe-list, orphan-property, malformed/unclosed-drawer, nested/unclosed-block, and unsupported-inline contexts without shadowing normal `a/i`, stock words, surround, or Snipe. With the pinned `org-special-ctrl-a/e=nil`, `I` uses column zero for real headings/items and indentation elsewhere, including list lookalikes in source blocks; `A` remains at literal line end. `M-h/l` targets one heading/list item/table column or prose word; in Visual state it changes every selected heading or contiguous safe list zone and retains the range. Visual `M-k/j` moves consecutive sibling subtrees or complete selected lines, including reverse and Block selections, and keeps the range on the moved text. Shifted Visual Meta commands use GNU Org's expanded-endpoint context and exit Visual state only after success. `M-H/L` uses tree/column scope; Normal- and Visual-state `M-K/J` handle a table row or the selected timestamp endpoint on a CLOCK line with duration repair, while Normal state also drags a literal non-CLOCK line. Visual CLOCK editing targets only the selection's moving endpoint and is one undo transaction. Plain shifted Left/Right also cycles property values through inherited/file-wide `NAME_ALL` declarations with quoted values, append semantics, checkbox fallback, wraparound, unsaved one-step undo, and fail-closed refusals; shifted Up/Down context-dispatches over ordinary timestamp fields and delimiters, heading priorities, direct list siblings, and vertical table-cell swaps with horizontal-rule skipping. Richer Org syntax and destructive repair outside the bounded parser, cross-entry `org-clock-history` coupling, unsafe mixed/tab-structured list regions, and clocktable horizontal Shift-arrow context remain gaps (`src/org/`, `scripts/org-test.sh`, `scripts/org-operator-test.sh`, `scripts/org-timestamp-test.sh`). |
| general (SPC leader) | ported/partial | normal and visual states share one described Space-leader keymap with exact binding verification and delayed continuation help; remaining capability gaps are listed in `docs/vi-parity.md` |
| vertico | ported/lem-builtin | prompt candidates open immediately without mutating input or eagerly accepting a synchronous singleton, show up to 20 rows, and cycle with `C-n`/`C-p`; `C-v`/PageDown and `M-v`/PageUp move by that configured page without wrapping. `Tab` inserts the focus while retaining the prompt, one `Return` accepts and submits, and `M-p`/`M-n` traverse prompt history. Prompt-aware `C-a/e/b/f`, `M-b/f`, `M-Backspace`/`C-Backspace`, `C-k/d/y/t`, `M-d`, `M-t`, `M-u/l/c`, `M-\`, `C-q`, `C-x Backspace`, `C-SPC`/`C-x C-x` with `C-w`/`M-w`, `M-y`, and GNU undo on `C-/`, `C-_`, or `C-x u` preserve the configured non-Evil minibuffer editing and protected field boundary without dismissing completion. The shared editing commands also remain live in ordinary prompts with no completion provider. Word transpose and case conversion retain signed-prefix behavior, whitespace deletion honors the backward-only prefix, and sentence killing reverses direction for a negative prefix. Prompt-only dispatchers close ordinary Corfu and replay into the active major/minor-mode maps. A zero-result edit retains the prompt and the next deletion, undo, or further input can repopulate the same command, buffer, or path provider (`src/completion.lisp`, `scripts/completion-test.sh`, `scripts/prompt-completion-test.sh`, `scripts/auto-completion-test.sh`) |
| orderless | ported/partial | ordinary-buffer completion has escaped-space components, whole-query smart case, any-order literal/regexp filtering, and `~ % = ^ ! ,` affix dispatch through `M-Space`. `%` reproduces pinned directional character folding for Unicode diacritics, compatibility forms, and ASCII quote variants through candidate `filterText`. CL-PPCRE still differs from Emacs regexp syntax. The pinned `&` annotation style is minibuffer-only upstream and has no effective path in the configured Vertico-Prescient minibuffer pipeline, so adding it to Lem's ordinary popup would diverge (`src/orderless.lisp`, `src/auto-completion.lisp`, `patches/lem-completion-lifecycle.patch`, `scripts/orderless-completion-test.sh`) |
| marginalia | ported/partial | `src/annotations.lisp` adds bounded display-only metadata to the supported prompt categories: M-x shows active bindings and command documentation; `src/help.lisp` shows callable type/signature/docs, variable type/bounded or censored value/docs, and live face properties; ordinary/project buffers show state, size, mode, and path; ordinary/recent/project files show local modes, size, age/date, and conditional owner; `load-library` shows loaded state, literal ASDF version/description, and source directory; `load-theme` shows active state, inheritance, and direct role count; bookmark prompts show target type/path, exact line/column, and bounded nearby context without visiting the file. Exact `M-x describe-face` selects package-qualified live Lem attributes, annotates their effective foreground/background/bold/underline/reverse state, and opens a styled sample with source navigation. The popup uses Marginalia's 20-cell floor and 10-cell rounded left-alignment policy with terminal-cell-aware wide labels. Supported documentation/value/signature and path fields use the pinned window-relative budgets and right/left ellipsis direction. Exact acceptance, display-only filtering, non-file special-buffer modes, real foreign-owner fields, per-candidate metadata-failure fallback, stale-bookmark fallback, package-qualified identity, secret censoring, alignment, 64/120-column layouts, and live narrow/wide resize repaint with input and focus preservation are TUI-tested. ELPA package annotations have no truthful Nix-managed Lem category; per-field semantic styling within popup details and remote fields remain gaps (`src/annotations.lisp`, `src/help.lisp`, `patches/lem-completion-marginalia-layout.patch`, `patches/lem-completion-repaint.patch`, `scripts/completion-test.sh`, `scripts/prompt-completion-test.sh`, `scripts/help-test.sh`). |
| consult | ported/partial | `src/project.lisp` supplies persistent project MRU, tracked+untracked annotated file selection, bounded asynchronous regexp search, and Emacs-style switch dispatch on `SPC p f/g/p`. `src/project-history.lisp` makes automatic and stock project saves/removals atomic and merge-safe across simultaneous Lem processes, preserving MRU updates without resurrecting intentional removals. `src/project-picker.lisp` makes `SPC SPC` a grouped Project Buffer/File/Root picker with fixed source order, `b/f/r Space` narrowing, empty-Backspace widening, group rotation, identity-preserving duplicate labels, and reversible preview-on-move. `src/project-outline.lisp` reproduces the audited Emacs-Lisp-tree `consult-outline`: an exact, non-evaluated `.dir-locals.el` declaration activates Normal/Emacs-state `C-c i`, line-numbered source-order headings, Prescient filtering, reversible recentered preview, one-key abort, match-column acceptance, and Vi jumplist return. Every accepted project-buffer and outline destination recenters and uses the configured four-stage Pulsar fade; previews and aborts stay silent (`src/jump-feedback.lisp`, `scripts/project-navigation-test.sh`, `scripts/project-outline-test.sh`). The switch menu preserves `f/g/d/v/e/o`: `v` opens Git through Legit at the selected root, while `e` uses Lem's rooted terminal and `o` uses rooted M-x-style command execution, approximations of `project-eshell` and `project-any-command`. Unopened-file previews deliberately remain raw UTF-8 text, avoid file/mode/switch/kill hooks, and skip undecodable, binary, or over-1-MiB files because generic Lem major-mode activation is not isolated from parser and hook side effects; ordinary window-display hooks can still run. |
| consult-eglot | ported | `src/workspace-symbol.lisp` gives `SPC p s` one `LSP Symbols:` prompt with Consult's 3-character minimum and 0.2/0.5-second debounce/throttle defaults, query-preserving history, project-scoped fan-out across every ready symbol provider, progressive score-ranked responses, per-server failure isolation, all-request cancellation plus stale-generation rejection, server-order Prescient filtering for score ties, kind/container/root-relative-file annotations, kind groups, the pinned case-sensitive kind-key-plus-Space narrowing map and empty-Backspace widening, reversible recentered preview, exact C-g rollback, one-Return acceptance, configured four-stage Pulsar destination feedback, and Vi `C-o` return (`src/jump-feedback.lisp`, `scripts/lsp-project-test.sh`). |
| imenu (built-in) | ported/partial | `src/imenu.lisp` registers ordinary `M-x imenu`. Ready Eglot-style document-symbol providers override the local index, supporting both hierarchical `DocumentSymbol` and kind/container-grouped `SymbolInformation`; Lisp-family buffers use the pinned GNU generic-expression form sets. `src/native-imenu.lisp` adds the pinned native Org depth-two tree, Markdown ATX/Setext/Footnotes tree, Python tree-sitter function/class hierarchy, categorized Java sparse trees, C's top-level Enum/Struct/Union/Variable/Function categories, C++'s additional Class/member hierarchy, and Rust's ordered Module/Enum/Impl/Type/Struct/Fn sparse trees, including normalized labels, structural exclusions, parent self-jumps, async definitions, Java's record-under-`Enum` mapping, C-family declarator/prototype semantics, qualified C++ names, trait-qualified Rust impl labels, and folded Org reveal. The default unflattened successive prompts, dot-for-space labels, Prescient filtering, exact source jump, recenter-only hook behavior, and Vi `C-o` return are physically tested. Native indices for other non-LSP modes remain absent (`scripts/lsp-project-test.sh`, `scripts/project-outline-test.sh`). |
| calc (built-in) | ported/partial | `M-x calc` opens a persistent GNU-style RPN stack in a compact bottom window and Evil Normal state. The mode keeps the configured precision-12/degree defaults, transactional non-Evil digit and algebraic prompts, and the common Evil-Collection arithmetic, unary, stack, undo/redo, copy/yank, angle, precision, help, and quit bindings. Evaluation uses packaged `qalc` through bounded direct argv; malformed input does not mutate the stack, and `q` restores the exact origin (`src/calc.lisp`, `scripts/calc-test.sh`). GNU Calc matrices/vectors, symbolic rewrite and algebra menus, custom unit tables, programming, graphing, trail, embedded, and keypad interfaces are absent, and qalc expression/format semantics can differ. |
| so-long (built-in) | ported/partial | `src/so-long.lisp` reproduces the configured global default for subsequently visited programming, CSS/XML, and fundamental-equivalent files: a line strictly over 10,000 UTF-8 bytes enters a wrapped, read-only basic mode before parsers, LSP, lint, gutters, DAP, or Paredit activate. `C-c C-c` restores the selected mode and presentation, while exact `M-x global-so-long-mode` toggles future visits. Plain-text and document modes remain outside the default target set (`scripts/so-long-test.sh`). Lem deliberately protects before original-mode activation instead of selecting that mode and deferring invisible buffers until display; decoded text is measured as UTF-8 rather than Emacs's internal representation. GNU So Long's action menu, file/directory-local customization, manual action alternatives, `longlines-mode`, and minor-mode-only actions remain absent. |
| large-file-warning-threshold (built-in) | ported/partial | `src/large-files.lisp` and `patches/lem-before-find-file.patch` enforce the configured strict 50 MiB boundary before a new local regular-file buffer is allocated or read. The prompt accepts `y` for ordinary decoding/hooks, `n` for allocation-free abort, and `l` for Latin-1 byte projection with fixed-LF handling, Fundamental mode, and no find-file hooks; unchanged bytes round-trip through save and external revert (`patches/lem-safe-revert.patch`, `scripts/large-file-test.sh`). Lem's literal buffer is character-backed rather than Emacs-unibyte, temporary implementation reads deliberately bypass interactive prompting, reopening a literal buffer does not offer Emacs's normal-versus-literal revisit question, and `?` gives concise help instead of the full Emacs help buffer. Remote files are outside Lem's local-file model. |
| corfu (TTY via Emacs 31) | ported/partial | Lem has an ncurses popup, correct display/filter/insert metadata, tracked ranges, stale-result rejection, the configured 3-character/0.2-second automatic trigger, provider-aware `preselect=valid`, exact-candidate fronting, non-cycling navigation, a display-only selected-candidate preview, selected `Tab`/`Return`, commit-before-input, non-accepting `M-Space`, staged `Escape`, one-stage `C-g`, and undo-honest input reset. The default prompt-boundary controls work on `C-a`/Home and `C-e`/End, while `C-v`/PageDown and `M-v`/PageUp move by the configured ten-row page without cycling. `M-Tab` expands the common prefix without accepting the implicit preselection and accepts an explicitly previewed candidate. `M-h` promotes available provider-rendered documentation into a transient ordinary split, and `M-g` displays the configured Cape file candidate's existing target; both restore the prior layout before the next command and refuse providers lacking the requested metadata. The preview remains a safe one-row float rather than an inline overlay and is suppressed for unrepresentable multiline/wrapped edits (`src/auto-completion.lisp`, `patches/lem-completion-lifecycle.patch`, `patches/lem-completion-validity.patch`, `patches/lem-completion-observer-change-group.patch`, `patches/lem-completion-presentation-focus.patch`, `scripts/auto-completion-test.sh`, `scripts/completion-lifecycle-test.sh`). |
| cape | ported/partial | automatic same-major-mode dabbrev and path-aware file-at-point fallbacks are context-dispatched and TUI-tested; recognized paths use file completion while other tokens use dabbrev, rather than merging the two nonexclusive Cape pools. Dabbrev retains prefix-only matching and the pinned Unicode-aware initial-cap/all-caps replacement policy before candidates feed Orderless. Cape's broader provider library is not ported. |
| yasnippet (+ snippets) | ported/partial | the configured private Org `jjs` source-block snippet is exact, and `src/snippets.lisp` supports 2,327 of the 2,387 definitions at pinned `yasnippet-snippets` commit `606ee926df6839243098de6d71332a697518cb86`; numbered, anonymous, and nested fields, defaults, repeated placeholders, ordinary and bounded pure-transform mirrors, escapes, `${0:...}`, `$0`, safe indentation directives, 62 safe backquote-bearing definitions, six common context conditions, nine literal-choice templates with Prescient selection and pinned initial-field `yas-auto-next`, exact comma-list transforms in both pinned Nix package templates, field navigation, bounded undo/redo field-session revival, a Prescient `M-x lem-yath-insert-snippet` selector, and common Eglot LSP snippet sessions work. The translator never reads or evaluates snippet Lisp, LSP backquotes remain literal, and 60 definitions requiring unsupported helpers, side effects, embedded code, mode-specific transforms, or conditions remain unavailable. |
| prescient (+vertico-) | ported/partial | prompt filtering reproduces the pinned default literal/regexp/initialism methods, whole-query smart case, and directional character folding for diacritics, compatibility forms, and ASCII quote variants. Persistent recency/frequency ranking is implemented. The exact Vertico-Prescient `M-s a/f/i/l/P/p/r/'/c` map changes all seven methods plus character/smart-case folding prompt-locally, refreshes immediately, supports `C-u` exclusive selection, guards the sole method, and resets on the next prompt (`src/completion.lisp`, `scripts/completion-test.sh`, `scripts/prompt-completion-test.sh`). CL-PPCRE regexp syntax and unusual Unicode word-boundary classifications can still differ from Emacs. |
| embark (+consult) | ported/partial | `SPC e a` opens a typed, extensible action dispatcher for contiguous regions, including forward and reversed Evil Visual Blocks with the pinned Evil-expanded linear text, URLs, existing local files, identifiers, buffers, native mode menus, focused completion candidates, and search/location rows. Outside completion, all unique targets are retained in provider order; repeating the same `SPC e a` chord cycles and wraps before the next one-key action. URL/file external opening uses an exact argv element, buffer copy/save/revert/kill is live, and a ready project can request and execute LSP code actions while a starting workspace withholds them. Completion-local `C-c a` can copy in place or accept once (`src/actions.lisp`, `scripts/actions-test.sh`, `scripts/lsp-project-test.sh`). Act-all, collect/export/live views, arbitrary Embark action-map composition, and richer embark-consult adapters remain gaps. |
| wgrep | ported/partial | Configured `M-s g` directory grep and project grep results open read-only. `M-s g` uses the exact `rg -nS --no-heading ` prompt with smart case and native ignore handling, while `SPC p g` searches the project's exact tracked-plus-untracked set asynchronously. Evil-Collection `i` (or upstream `C-c C-p`) enters an isolated editable stage; pending rows are highlighted without touching their sources. `ZZ`, Evil-Collection `:w`, `C-c C-c`/`C-c C-e`, or `C-x C-s` apply validated rows to dirty source buffers without saving, while `ZQ`/`C-c C-k` rolls the result transaction back and `C-x C-q`/normal Escape exits with the pinned apply-or-discard question. Apply groups each source buffer's edits, retains ordinary source-buffer saving, visibly rejects a row whose source changed after the grep, and atomically refuses multiline input. Point-preserving source updates and lifecycle-owned preview timers keep rapid Vi editing stable (`src/project.lisp`, `patches/lem-grep-writeback.patch`, `patches/lem-peek-source-timer.patch`, `scripts/daily-workflows-test.sh`, `scripts/project-navigation-test.sh`). Wgrep's editable header/newline toggle, whole-line deletion, per-region unmarking, multi-line replacement, automatic save, and per-row error echo remain absent. |
| compile / ansi-color-compilation-filter (built-ins) | ported/partial | `SPC c c` runs an asynchronous Bash command in the originating buffer's directory with its captured Direnv/process environment. Each origin buffer remembers its prompt command, initially the exact Emacs 31 `make -k -jN ` default (`N = ceiling(2 * nproc / 3)`); the installed runtime includes Make, and the preflight supplies `save-some-buffers`-style `y/n/!/./q/d`, including a bounded unified diff. The fixed read-only `*compilation*` buffer streams merged output through a stateful ANSI decoder, recognizes representative GCC/Clang, Rust/Cargo, Go, Python/pytest/Ruff, and Nix locations, and installs the effective Evil Collection navigation (`gj/gk`, `C-j/C-k`, `Tab`/`S-Tab`, `[[`/`]]`, `go`, `Return`), `gr`, and `C-c C-k`; `go` preserves the log if the remembered origin window was deleted. Global `M-g n/p` follows the most recently selected compilation or buffer-diagnostic provider. A pinned, minimal-environment Python guardian broker receives the command and captured environment over private framed stdin, keeping project values out of its argv/environment. Separately pinned and cached runtime paths prevent project `PATH` shadows from replacing Python, Bash, or `nproc`. Lem sends `INT`, `KILL`, and `RELEASE` over the private broker pipe and retains that pipe plus a locked armed-state Boolean—Lem neither stores nor signals the command PGID. The broker and watchdog stay outside a separately anchored live command group; the watchdog parents and pins its anchor, kills the group after broker-only death, and leaves control responsive when the command stops its immediate parent or whole group. `C-c C-k` requests group SIGINT; the reader asynchronously applies bounded SIGKILL escalation, reports completion, and reaps the broker. Replacement, buffer kill, source reload, and editor exit reject stale events, synchronously terminate validated same-group descendants, join the reader, and reap the broker. Normal terminal status also reaps it at a bounded underfull drain point without waiting for a descendant that retains or continuously writes inherited stdout (`src/compilation.lisp`, `src/compilation-guardian.py`, `scripts/compilation-test.sh`). The parser and SGR repertoire are finite; there is one active fixed-name session, Bash and UTF-8 are assumed, output/diff/control/environment/command data are bounded, descendants outside the validated group are not signaled, Linux `memfd_create` plus `/proc/self/fd` are required, and Comint interaction, prefix behavior, and custom compilation names are absent. |
| server / emacsclient (built-ins) | ported/partial | The packaged `lemclient` sends bounded owner-local Unix-socket requests to the existing configured ncurses process (`src/server.lisp`, `scripts/lemclient.sh`). It supports blocking and no-wait file opens, `+LINE:COLUMN`, multi-file completion, current-pane attachment, save/clean finish, recoverable abort, and visible tmux-pane handoff. The socket directory is 0700, socket and pane metadata are 0600, non-socket and symlink metadata fail closed, pane IDs are bound to their publishing tmux server, and clean exit removes only owned artifacts (`scripts/server-test.sh`). The editor sets `GIT_EDITOR` and otherwise-unset `VISUAL`/`EDITOR` for child processes only. This reuses one ncurses pane rather than providing a headless daemon, arbitrary evaluation, or graphical/new terminal frames; without a reachable reusable pane the client starts a fresh Lem. |
| eglot + eglot-booster | lem-builtin+ported/partial | `lem-lsp-mode` uses a native JSON-RPC client, so Eglot Booster is unnecessary. The installed application packages rust-analyzer, pyright, harper-ls, csharp-ls, nixd, gopls, terraform-ls, JDTLS, `nixfmt` from nixfmt-rfc-style, and google-java-format; installed-wrapper ncurses gates perform real handshakes and process-cleanup checks. GDScript connects to the running Godot editor over external TCP at the project-derived settings port without spawning a process. Stdio servers use real pipes with stderr isolated from JSON-RPC. Rust, manual Python, Markdown, C#, Nix, Go, and Terraform prefer the containing Git root exactly like the configured Git-only project.el backend, including merged submodules and separate linked worktrees, then retain language-marker fallback outside Git; Go and Terraform override upstream Lem with Eglot's stdio commands and no Lem-only Go initialization options. Lem advertises and answers `workspace/configuration`, supports dynamic `workspace/didChangeWatchedFiles` registration through bounded Linux inotify with Eglot globs, `RelativePattern`, kinds, open-buffer suppression, new-directory growth, and lifecycle cleanup, advertises and tracks work-done progress, and supports LSP 3.17 pull diagnostics. Eglot-style simultaneous-task percentages are averaged in every attached buffer's modeline, with independent two-second completion expiry; the terminal has no graphical modeline hover for task titles and messages. Ordinary local-file workspaces are isolated by stable server identity + canonical root, pending starts deduplicate and time out, save-as/mode changes rebind explicit ownership, restart/shutdown is project-scoped and graceful, and server cwd, initialization options, and workspace configuration are frozen to the project. Python deliberately retains a registered, manually enabled Pyright spec because the Emacs configuration has no Python Eglot hook. The remaining root-policy difference is language-marker rather than transient-directory fallback outside Git. The configured Emacs path has Corfu Popupinfo disabled, ignores completion commands, and does not advertise CompletionList defaults or `insertTextMode`, so those are intentionally not added as Lem parity targets. |
| flycheck (+rust) | ported/partial | `src/lint.lisp` enables a cancellable checker in programming buffers outside LSP ownership, runs on mode enable/save/newline/500 ms idle change, and reuses LSP overlays, popup, list, and navigation. Packaged chains cover Ruff then Mypy, Clang, Cargo `test --no-run`, gofmt/vet/build-or-test, Bash syntax, `python -m json.tool`, and `nix-instantiate --parse`; SOPS plaintext, temporary, and read-only buffers are refused. `C-c ! c/n/p/l` preserves the effective Flycheck bindings; global `M-g n/p` routes between the most recently selected compilation provider and the current buffer's linter/LSP diagnostics. The arbitrary Flycheck checker catalog, checker selection/verification, compiled output buffer, and explain/copy commands are not ported. |
| apheleia | ported/partial | `SPC b f` formats unsaved buffer text through a mapped CLI/in-process backend, with ready LSP fallback only when manual formatting has no usable mapped backend; mapped programming modes with an available backend format synchronously before the ordinary save, so the final text reaches disk through one write before LSP `didSave`. Formatter hunks, ws-butler, and EditorConfig normalization form one retained transaction: formatter hunk read-only conflicts are rejected before mutation, later normalization conflicts or safe hook/finalizer errors roll back coherently, and an unsafe rollback aborts saving with visibly dirty truncated history. A failed formatter contributes no output, but ordinary save normalization still runs (`src/formatting.lisp`, `patches/lem-after-change-undo.patch`, `scripts/formatting-test.sh`, `scripts/vundo-test.sh`). The registry is broad but finite, and async formatting, formatter prompts, and Apheleia-style per-project backend overrides are absent |
| dape (DAP debugging) | ported/partial | `src/dap.lisp` supplies the exact stock `C-x C-a` prefix map, one foreground DAP session, global in-memory source/function/conditional/hit/log breakpoints, threads, stacks, scopes, variables, watch/evaluate/REPL views, stepping, run-to-cursor, restart, memory/disassembly requests, source navigation, and an interactive Lem shell for `runInTerminal`. Packaged presets launch debugpy, Delve, LLDB, and GDB directly for Python, Go, Rust, C, and C++; the installed gate uses every real adapter (`scripts/dap-test.sh`). Nested `startDebugging`, concurrent sessions, shell-interpreted terminal requests, a general configuration editor, and breakpoint persistence beyond the effective Emacs setup remain absent. |
| treesit-auto / tree-sitter-langs / tsc / grammars | ported/partial | `src/tree-sitter.lisp` automatically installs a distinct parser in eligible buffers for 24 packaged grammar/query pairs: Bash, C, C++, C#, Clojure, CSS, Go, GDScript, HTML, Java, JavaScript, JSON, Just, Lua, Markdown, Nix, Nu, Python, Rust, TOML, TypeScript, TSX, Typst, and YAML. It retains each mode's normal indentation and falls back to its TextMate parser on activation failure. Query highlighting approximates Emacs font-lock level 3; injection/locals scope processing and incremental reparsing remain gaps (`scripts/tree-sitter-test.sh`). |
| nix-mode | lem-builtin+ported/partial | Packaged **nixd** with flake-derived nixpkgs/options and packaged `nixfmt` from nixfmt-rfc-style (`src/ide.lisp`). Its workspace uses the configured project.el Git root and the nearest Nix marker only outside Git. Like Emacs, Lem freezes an outer `nixd` settings map and returns its requested section through `workspace/configuration`; the remaining environment difference is that Emacs's declared daemon PATH normally supplies no formatter candidate while Lem packages nixfmt-rfc-style. |
| rust-mode | lem-builtin+ported/partial | Packaged **rust-analyzer**, cargo, rustc, rustfmt, and clippy auto-start and are installed-wrapper tested at the configured project.el Git root; outside Git, Lem usefully falls back to the nearest Cargo.toml rather than Eglot's transient buffer directory. |
| go-mode | ported/partial | Packaged **gopls** is handshake-tested through the configured stdio spec at the project.el Git root with no upstream Lem-only initialization options; outside Git, Lem falls back to the nearest go.mod rather than Eglot's transient buffer directory. |
| markdown-mode / markdown-ts-mode | ported/partial | Packaged **harper-ls --stdio** is handshake-tested. Lem has one Markdown mode rooted at `.git`; Emacs hooks both ordinary and tree-sitter modes at their project.el roots. |
| csharp-mode / csharp-ts-mode | ported/partial | `.cs` and `.csx` files use a native C# mode with C-like indentation, automatic query-based tree-sitter highlighting with a TextMate fallback, packaged **csharp-ls** at the nearest `.sln`, `.csproj`, or `.git` root, pull diagnostics, work-done progress, and dynamic filesystem-watch notifications. Installed-wrapper gates compile the C# grammar/query and obtain a real semantic error. Tree-sitter does not drive indentation or the wider mode semantics. |
| c++-mode / c++-ts-mode | ported/partial | `.cc`, `.cp`, `.cpp`, `.cxx`, `.c++`, `.hh`, `.hpp`, `.hxx`, and `.h++` select a distinct C++ major mode inheriting C editing/formatting behavior, with the packaged C++ parser and combined C/C++ highlights. The physical Imenu gate proves suffix routing, parser identity, class/member hierarchy, qualified names, exact jumps, and C-family exclusions. Tree-sitter does not drive indentation. |
| (python via pyright) | ported/partial | Packaged **pyright-langserver --stdio** remains registered and is handshake-tested after explicit `M-x lsp-mode`. It does not auto-start, matching the absence of a Python Eglot hook; Ruff and Mypy provide default Python diagnostics until Pyright is enabled. |
| terraform-mode | ported | Packaged **terraform-ls serve** is handshake-tested over stdio at the configured project.el Git root, with the same buffer-directory fallback outside Git. |
| clojure-ts-mode / cider | lem-builtin+partial | Lem supplies Clojure mode, tree-sitter highlighting, clojure-lsp integration, and an nREPL REPL. Exact CIDER parity is not a target: CIDER is installed in the Emacs profile but has no explicit configuration. |
| eglot-java | ported | `M-x lem-yath-java-lsp` manually starts packaged JDTLS without a Java hook, sends the configured Google Java style URL, and isolates JDTLS data by canonical project root. Packaged google-java-format backs `SPC b f` and format-on-save; the real-server and formatting TUI gates cover both paths. |
| gdscript-mode | ported/partial | `.gd` selects a programming mode with the pinned comments, width-4 tab indentation, packaged parser highlighting, and Expreg. Its automatic `project.godot` workspace reads the matching Godot editor-settings port (default 6005), connects to the already-running external TCP server, and fails without damaging the editor when Godot is absent. The wider upstream mode's Godot run/debug/docs/scene commands and exact indentation/navigation are not reproduced (`src/language-modes.lisp`, `src/ide.lisp`, `scripts/gdscript-test.sh`, `scripts/tree-sitter-test.sh`). |
| nasm-mode | lem-builtin | `lem-asm-mode` |
| just-mode / meson-mode / nginx-mode / nushell-ts-mode / typst-ts-mode | ported/partial | `src/language-modes.lisp` reproduces the pinned filename associations, case-insensitive Justfile forms, nginx directory and magic-content detection, Nu shebangs, comment syntax, and configured indentation widths. Just, Nu, and Typst use packaged tree-sitter queries; Meson and nginx use bounded TextMate highlighting. Full Meson SMIE, upstream completion/Imenu, tree-sitter indentation, and language-specific editing commands remain absent (`scripts/tree-sitter-test.sh`). |
| yaml-mode | lem-builtin | `lem-yaml-mode` |
| sqlite3 | n/a | elisp FFI library |
| lispy / lispyville | ported | Paredit in Common Lisp, Clojure, Scheme/Racket, and Elisp; delimiter-safe Vim operators, atom motions, slurp/barf, drag, splice, split, raise, transpose, convolute, and list insertion/opening (`src/structural.lisp`, `scripts/structural-test.sh`); wrapped-row delimiter safety and Lispyville's screen-row character-register quirk are covered by `scripts/screen-line-test.sh`; plus full SLIME via micros |
| magit | lem-builtin+ported | `lem/legit` supplies status, file/hunk stage and unstage, commit, branch, push/pull, stash, log, and rebase on `SPC g G`. `src/git.lisp` makes the diff/commit/rebase maps win over Vi state, repairs complete temporary Git hunk patches, and avoids the successful-commit kill prompt. The installed-wrapper TUI gate exercises every core mutating workflow except rebase against a bare remote and independent peer clone (`scripts/vcs-test.sh`). |
| magit-todos | ported/partial | `src/git.lisp` adds a navigable TODO/FIXME section to Legit Git status through a small pinned-upstream status hook. The synchronous `git grep` scan is deliberately limited to tracked nonbinary files, 200 results, 1 MiB, and five seconds; it does not reproduce magit-todos keyword customization or grouping (`scripts/vcs-test.sh`). |
| forge | ported/partial | `M-x lem-yath-forge` uses packaged `gh` and its existing credential helper to list, inspect, create, comment on, close, reopen, and externally browse GitHub.com pull requests and issues. Multiline issue/PR/comment composition stays inside Lem, while an explicit-refresh memory cache adds navigable PR/issue previews to Legit without network I/O during ordinary status redraws. The Emacs configuration disables Forge's direct bindings, so this port likewise adds no global/leader chord. GitLab, GitHub Enterprise, notifications, labels/assignees, reviews/merge, and a persistent offline Forge database remain absent (`src/forge.lisp`, `scripts/forge-test.sh`). |
| git-gutter | lem-builtin+ported | `src/git.lisp` wraps `lem-git-gutter` in a buffer-local programming-mode lifecycle. The installed-wrapper VCS gate proves real add/modify/delete markers in a linked worktree, exclusion from prose/utility buffers, composition with other gutters, and no reserved blank column for a clean line. |
| git-timemachine | ported/partial | `SPC g t` opens rename-aware history at the translated source point; `C-k`/`C-j`, `g t g`, `g t t`, `g t y`, `g t Y`, `g t b`, and `q` reproduce the complete configured Evil-collection map under `scripts/vcs-test.sh`. Short/full hashes enter Lem's kill ring and clipboard. Blame runs against the selected revision's historical path in a focused read-only child view whose `q` restores the unchanged history buffer, rather than using Magit's inline overlays. |
| majutsu (jj) | ported/partial | packaged `jj` powers smart `SPC g g` dispatch and forced `SPC g J`. The bounded row-aware porcelain matches the everyday Evil keys for adjacent-row plus working-copy/parent/child navigation, shared multiline describe (`c`) and working-copy commit (`C`) editing, prompt-based `o` plus direct `O`/`I`/`A` child/before/after creation, edit, undo/redo, confirmed abandon, diff/show, refresh, help, and nested quit restoration. `a` opens pinned Majutsu-style absorb with selected-row or arbitrary from/into revsets, one arbitrary fileset expression, immutable override, clearing, cancellation, direct-argv execution, and initiating-row restoration. `s` opens Majutsu-style squash configuration with selected-row or arbitrary revision/from/into/destination/after/before endpoints, one fileset expression, keep-emptied and immutable controls, selection clearing, cancellation, and direct argv. The `s s` default still moves the selected change into its parent and accepts jj's full combined description. `i` opens a bounded native selector with file, hunk, and within-hunk changed-line controls; its private tool moves only the selected patch, retains the source and both descriptions when changes remain, and restores the initiating row. `r` opens a confirmed selected-row rebase popup with branch, subtree, exact-revision, insert-after, and insert-before modes; its Prescient history prompt also accepts arbitrary revsets, both cancellation points are non-mutating, and success preserves the source row. `_` opens Majutsu-compatible revert configuration with selected-row source/after defaults, arbitrary source and destination revsets, onto/after/before placement, reset, cancellation, direct-argv execution, and source-row restoration. `R` opens restore with Majutsu's argument-free working-copy default, selected-row or arbitrary from/into/changes-in revsets, one arbitrary fileset/path, descendant-content preservation, immutable override, selection clearing, cancellation, direct-argv execution, and row restoration. Its `- i` route stays inside Lem in a bounded read-only selector with whole-file, whole-hunk, and within-hunk changed-line controls; a private direct-argv complement tool restores only the selection and preserves unselected changes. Majutsu-compatible `y` opens selected-row duplicate placement for the existing parent, onto, after, and before destinations, while `Y` performs the parent case immediately; annotated revset completion, content preservation, failure handling, and original-row restoration are covered end to end. `S` opens a bounded read-only Git-diff selector with whole-file, whole-hunk, and within-hunk changed-line selection, hunk navigation, onto/after/before placement, existing-parent reset, parallel layout, explicit execution/cancellation, empty-selection refusal, and change-ID row restoration. Its private direct-argv tool is exercised by a real partial split that separates one of two replacements. Local bookmarks render on history rows, while `b` supplies Majutsu's local `l/c/s/m/M/r/d/f` list, create/set, move, rename, delete, and forget lifecycle with confirmation and point preservation (`src/git.lisp`, `scripts/jj-porcelain-test.sh`). Majutsu's general transient dispatch, remote bookmark tracking/advance and multi-bookmark operations, multi-source/destination duplicate selection and configurable duplicate descriptions, repeated merge-placement squash values and shared diff-buffer selection sessions, shared multi-selection sessions and advanced rebase flags, binary/conflict and word-level patch selection, partial added/deleted-file line selection, conflict, workspace, sparse, operation-log, multiple separately completed restore paths, and wider arbitrary-revision/fileset/tool split surfaces remain absent. |
| org (capture) | ported/partial | `SPC o` opens the configured one-key `i/t/p/r` selector and an editable Org capture buffer at `%?`; `%i` preserves an active Visual selection and `%a` supplies a bounded local file/line link. `C-c C-c` inserts the complete edited fragment into the exact inbox/TODO/public/reading target and saves its live target buffer, `C-c C-k` aborts, `C-x C-s` is guarded, and either path restores the exact source point and Vi state (`src/notes.lisp`, `src/org-capture.lisp`, `scripts/notes-test.sh`). This deliberately implements the four active templates rather than Org's arbitrary template language and limits stored-link annotations to local files. |
| org (editing) | ported/partial | GNU Org's in-buffer `C-c C-s` and `C-c C-d` chords insert or replace SCHEDULED/DEADLINE fields below the current heading without implicitly saving. An active Evil-Org region maps the command over every selected nested headline with one prompt per headline; `C-z` preserves its linewise shape for Emacs-state universal-prefix removal, and later cancellation retains earlier edits like pinned GNU Org. Their shared `org-read-date` analogue accepts ISO, partial numeric, English month/weekday, ISO-week, today/tomorrow/yesterday, and single/double-relative forms beside an adaptive one/three-month terminal calendar; Org's day/week/month/quarter/today prompt keys update the selection. Rescheduling preserves time/repeater/warning suffixes; one prefix removes the field, while two prefixes choose and replace its deadline-warning or scheduled-delay cookie. `C-c .`/`C-c !` reuse the reader to insert or replace active/inactive ordinary timestamps with optional times/ranges, suffix preservation, prefix-current-time forms, successive-command active/inactive date ranges, and one-step undo. `Shift-Left`/`Shift-Right` plus terminal-safe `C-c Left`/`C-c Right` shift timestamp dates, cycle heading TODO states, cycle complete list levels through GNU Org's configured bullet order, cycle inherited/file-wide property values including quoted text and checkbox fallback, or swap neighboring table-cell contents by context. `Shift-Up`/`Shift-Down` plus `C-c Up`/`C-c Down` adjust cursor-selected ordinary timestamp fields or delimiters, cycle A/B/C heading priorities, navigate direct list siblings, or swap table cells vertically while skipping horizontal rules. Timestamp ranges retain GNU rounding, prefix, overflow, and endpoint-coupling semantics; priority and property edits remain unsaved. Bullet-width changes retain child/continuation structure and ordered numbering; table formulas remain attached to their coordinates, and unsafe list shapes or table edges refuse before mutation. The active Evil-Org `C-Shift-h/l/k/j` routes select the sole TODO keyword set or synchronously shift cursor-selected CLOCK year/month/day/hour/minute fields with GNU five-minute rounding, exact prefixes, duration repair, calendar overflow, and open-clock fallback. Since traditional ncurses input collapses Ctrl-Shift letters, `C-c H/L/K/J` provides an accessible non-conflicting spelling. Normal-state `M-K/J` adjusts one CLOCK endpoint and recomputes duration; cross-entry clock-history coupling remains outside the bounded model. `C-c '` opens a block body in its configured language mode with preserved indentation, GNU protection-comma conversion, and logical point mapping; the edit map supplies GNU `C-c '` commit/exit, `C-c C-k` abort, and `C-x C-s` writeback/save-without-exit behavior. Ordinary commit remains unsaved and one Org-buffer undo step; concurrent source changes and read-only buffers fail closed (`src/org/date-reader.lisp`, `src/org/source-editing.lisp`, `scripts/org-source-edit-test.sh`). The packaged ncurses gates also cover the physical calendar, planning/timestamp defaults and prefixes, named/relative input, cancellation, read-only refusal, persistence boundaries, and production routes (`src/org/planning.lisp`, `src/org/commands.lisp`, `scripts/org-planning-test.sh`, `scripts/org-timestamp-test.sh`). Clocktable horizontal context, mouse selection, Org's exact live echo overlay, other timestamp variants, and prefixed live Babel-session editing remain gaps. |
| org-roam | ported/partial | bounded fresh file+heading node index with ID/title/alias/inherited-tag completion, exact find/random visits, Org ID insertion, and five-template capture finalize/abort (`src/roam.lisp`, `src/roam-capture.lisp`, `scripts/roam-test.sh`). `M-x org-roam-buffer-toggle` adds the configured persistent 0.4-width right-side panel with source-title sorting, outlines/previews, every ID backlink, citation-key and HTTP(S) `ROAM_REFS` reflinks across Org and Markdown, cheap nearest-node following, exact stale-safe Return visits, coalesced refresh after in-editor saves while visible plus manual `g` for out-of-band changes, `q`, foreign-side-window ownership, and reload cleanup (`src/roam-backlinks.lisp`, `scripts/roam-backlink-test.sh`). The graph remains an in-memory snapshot: no persistent SQLite DB, always-on incremental autosync, or arbitrary third-party reference schemes. |
| md-roam | partial | pinned front-matter ID/title/`ROAM_ALIASES`/Zettlr-tag indexing, `[[Title]]` insertion, and the configured YAML-front-matter Markdown capture template share the roam workflow. Unique title/alias wiki links participate in the same backlink panel, while `C-c C-o` follows unique title/alias/ID targets or starts non-inserting capture for a missing target; the Emacs-state universal prefix opens an existing target in another window, and ambiguous or non-semantic syntax fails closed. Markdown inline-file backlinks and a persistent mixed-format graph remain absent. |
| org-roam-dailies | ported | `SPC n r d t` opens today directly; `SPC n r d d` shares the Org-style named/relative reader and terminal calendar, then visits the configured `%Y-%m-%d.org` buffer with its exact title head (`src/notes.lisp`, `src/org/date-reader.lisp`, `scripts/notes-test.sh`). |
| org-journal | ported (configured path exact) | Normal or Visual `SPC n j j` opens `$WORKDIR/roam/journal/%Y%m%d.org`, creates or repairs the single `#+TITLE: %a, %Y-%m-%d` header, appends a blank-separated `* HH:MM ` prefix, and leaves the text-ready destination in Normal state. Repeated unsaved invocations reuse the live buffer and title (`src/notes.lisp`, `scripts/notes-test.sh`). The profile binds no wider org-journal interface. |
| org-agenda / org-super-agenda | ported/partial | `SPC m a` scans the same existing `$WORKDIR`, `$PUBLIC_ORG_DIR`, and public `mcp/` roots as Emacs, using only top-level non-hidden `.org` files; it groups overdue/today/upcoming/unscheduled TODO entries plus ordinary, timed, ranged, and repeating active-timestamp events, excludes COMMENT/ARCHIVE trees and non-semantic timestamp contexts, provides Return/`gr`/q navigation, exact other-window source visits on Evil-Org `Tab`/`g Tab`/Shift-Return, and count-aware source-item motion on `gj`/`gk` and `C-j`/`C-k` that skips agenda decoration, with coalesced asynchronous refreshes that snapshot modified live Org buffers before background parsing, matches Evil-Org agenda `t` with the configured one-key TODO selector and `J`/`K` with GNU Org's default A/B/C priority transitions, deletes complete subtrees through Evil-Org `dd` or GNU `C-k`, sets validated Effort properties through `ce`, `e`, or `C-c C-x e`, and shifts planning/event timestamps through `H/L` or GNU shifted-arrow routes with day/hour/five-minute prefix continuation and range preservation. Evil-Org `p` (GNU `>` in Emacs state) edits the exact planning/event timestamp through the shared date reader, preserves its delimiters, time range, repeater, and warning syntax, and remains one unsaved source-buffer undo step like the configured Emacs command. Evil-Org `u` remotely undoes the newest registered source-buffer group without saving, so saved mutations leave disk at their post-command state, bulk actions unwind one processed row at a time, archive undo retains the saved destination, stock clock undo clears invalid runtime state, and explicit `gr` clears remote history. It preserves GNU Org's `C-c C-s`/`C-c C-d` schedule/deadline chords including C-z Emacs-state removal and warning/delay prefix forms, provides completion-backed, aligned local-tag replacement/clearing on Evil-Org `ct` and GNU Org `C-c C-q` (Return accepts the current valid tag expression), durably moves a complete subtree to the default adjacent `_archive` file on Evil-Org `dA` (`da` confirms) plus GNU Org's archive chords, and matches the current nil-target GNU `C-c C-w` policy by completing over same-file level-one headings and moving the selected subtree to the target's final-child position. Agenda `I/O` preserves the effective state split: Vi state controls one global Org-style clock, while C-z Emacs state starts concurrent clocks on the current or bulk-marked rows and stops marked clocks—or all semantic clocks across agenda files when nothing is marked. Evil-Org `cg`/`cc` and base `J`/`X` jump to or transactionally cancel the active clock; a jump falls back to its exact source in another window, while cancel removes an empty `LOGBOOK` and remains an undoable unsaved edit like GNU Org. Evil-Org `cr` and base `R` toggle an asynchronously collected, source-linked clocktable for the displayed agenda span, with Org-style running-clock exclusion, boundary clipping, reduced-level-two rollups, and per-file/all-file totals. Evil/base bulk mark keys render `>` and retain exact live source points across insertions and refreshes. Evil-Org `x` and base `B` prompt once for TODO, tag add/remove, schedule, deadline, default archive, or same-file refile across marked rows, with current-row fallback and success-only mark clearing. Evil-Org `sc/sr/se/st/s^/ss/S` and GNU `</=/_/\\/^/~/|` provide cached, display-only category/regexp/Effort/tag/top-headline filters, temporary per-section limits, visible stacked status, negative and accumulation prefixes, and refresh persistence over scan-derived category/tag/Effort/top-headline metadata. Evil-Org `gD` selects day/week/fortnight/month/year/reset spans, `[[`/`]]` move by the selected span with counts, `.` returns to today, and `gd` uses the shared Org date reader; non-summary spans render every date and clock reports follow their boundaries. Mutations validate source headings, planning fields, and ordinary timestamp tokens and normally save immediately. The configured `p` timestamp edit and clock cancel remain one-step unsaved source edits like Emacs; refile also rechecks the chosen target, and archive writes the destination before deleting the source and records Org's default context properties (`src/apps/agenda-undo.lisp`, `src/apps/agenda-undo-command.lisp`, `src/apps/agenda.lisp`, `src/apps/agenda-mutations.lisp`, `src/apps/agenda-archive.lisp`, `src/apps/agenda-refile.lisp`, `src/apps/agenda-clock.lisp`, `src/apps/agenda-bulk.lisp`, `src/apps/agenda-filter.lisp`, `src/apps/agenda-view.lisp`, `scripts/agenda-test.sh`, `scripts/agenda-undo-test.sh`, `scripts/agenda-clock-test.sh`, `scripts/agenda-bulk-test.sh`, `scripts/agenda-filter-test.sh`, `scripts/agenda-view-test.sh`). Diary sexps, hour repeaters, full time-grid/range presentation, exact scheduled-delay/deadline-prewarning reminder rendering, configurable/cross-file refile targets and creation/copy/reverse or prefix/cache variants, custom archive destinations/local archive sibling or tag commands, bulk archive-sibling/scatter/arbitrary-function/persistent-mark variants, custom numeric report spans and recent-task/prefix variants, general `/` matcher expressions, tag-group/preset/auto-exclusion filtering, the other `gD` display toggles, and the wider Org agenda UI remain gaps. |
| org-modern / org-download / org-ref / org-contrib / ob-async / ob-dsq / engrave-faces / cdlatex | partial | `src/org/modern.lisp` auto-enables a cell-stable, display-only terminal analogue of the active org-modern hook: fold-aware heading symbols, TODO/priority/tag labels, bullets, checkboxes, tables/rules, block and keyword markers, timestamps, and internal/radio targets, with source-block exclusion and a buffer-local `M-x org-modern-mode` toggle (`scripts/org-modern-test.sh`). `src/org/download.lisp` exposes the configured `org-download-yank` and `org-download-clipboard` commands with timestamped `$WORKDIR/media/` files, seconds-precise annotations, relative links, heading IDs, Wayland/X11 capture, bounded direct processes, signature validation, transactional cleanup, and one-step buffer undo (`scripts/org-download-test.sh`); retrieval is synchronous and ncurses has no inline image rendering. `src/org/babel.lisp` executes the configured Shell, Python, C/C++, Nix, SQLite, PostgreSQL, and pinned DSQ subset with the configured trusted-note confirmation boundary, bounded direct processes, atomic output/table results, and real ncurses coverage (`scripts/org-babel-test.sh`). DSQ supports ordered regular files, local/cross-file named Org tables, named source results, and its configured cache/number/header/hline/null/false rendering controls; arbitrary Elisp-valued inputs and Babel variables/sessions remain outside Lem. `src/org/publish.lisp` uses bounded Pandoc conversion for live-buffer HTML export and the configured recursive note/static/composite projects, with ID/file-link rewriting, MathJax, incremental/force/cancel behavior, atomic output, and packaged-editor coverage (`scripts/org-publish-test.sh`). Exact graphical org-modern typography, spacing, fringes, progress resizing, and agenda decoration; Emacs Lisp; async and richer result modes; bibliography workflows; in-editor image/LaTeX preview; non-HTML exporters; and exact `ox-html` markup remain gaps. |
| citar / ebib / reftex | ported (configured Citar path) | `SPC y o` searches the configured nodes/zotero BibTeX files in order and opens existing PDF/HTML attachments externally, ordinary files in Lem, validated HTTP(S) URLs externally, or in-root Org/Markdown reference notes. The installed-wrapper TUI gate covers physical dispatch, duplicate precedence, nested/escaped fields, Zotero and `file://` paths, exact direct browser argv, source preservation, and traversal/symlink refusal (`src/apps/citar.lisp`, `scripts/citar-test.sh`). Ebib, RefTeX, org-ref, and richer citation insertion remain gaps. |
| gptel | ported/partial | No-file startup is an Org LLM conversation with `C-c Return`; OpenRouter, Perplexity, GitHub Copilot Chat, native ChatGPT Codex Responses, the Grok OAuth proxy, and native CLI agents stream tagged replies at a tracked send position and add the next `* ` prompt. Later sends reconstruct separate user/assistant turns and render bounded Org user text, active regions, and source/result pairs as Markdown. Stateless providers receive that typed transcript, while resumable CLI and established OAuth sessions retain provider-owned history without duplication. Ordinary buffers retain the shared Markdown transcript, and read-only conversations fall back to it without source mutation. Requests and resumable sessions are buffer-local. Terminal-native User/Assistant gutters, an active-role modeline badge, assistant tint, a moving synthetic stream cursor, and a presentation toggle reproduce the configured gptel role visuals without mutating provider-visible text. `SPC g l` is the configured compact three-column Presets/Handoff/Advanced menu; compact `m` and direct `SPC g L` open a four-column full menu for the supported system, provider/model, temperature/token, tool-policy, preset, send/abort/session, request-trace, response-destination, and dry-run controls. One-shot `e`, `b`, `g`, and `k` route a completed response to the echo area, exact point in another buffer, an existing or new typed Org LLM session, or the kill ring; `.` restores the ordinary destination. Source buffers retain abort lookup for redirected requests; initial `g` requests keep the source provider context, while `C-c Return` from the resulting session reuses its typed history. `J` opens a read-only credential-free normalized JSON preview without dispatching. At an Assistant response, a conditional fifth column marks the exact span, regenerates transcript-backed providers with captured request settings, rotates bounded previous/next variants as one property-preserving undo group, and opens a terminal unified comparison; native resumable CLI histories refuse regeneration before mutation. Claude Code responses also retain provider session/message boundaries: conversation-local `C-c C-f` creates a locked, registered project JSONL fork at the nearest preceding Assistant response and `C-c C-b` selects an indexed project session. Sending before an existing same-session Claude continuation automatically performs the equivalent fork in Lem's linear transcript without reforking an already divergent branch. With a Visual region, conditional `r` stages a mode-aware provider rewrite in a selected read-only terminal preview while the tracked source remains unchanged; `A` accepts as one undo group, `K` rejects, `r` iterates, `D` shows a bounded unified diff, `M` inserts a conflict-marker merge, and `q` retains the pending highlighted proposal. `M-x lem-yath-llm-request-trace-toggle` and `M-x lem-yath-llm-request-trace-open` reproduce the configured opt-in lifecycle diagnostics in a selected read-only `*gptel-requests*` viewer, using bounded prompt previews and metadata-only chunk/terminal records. Buffer-local text context supports region, buffer, file, directory, and the audited Emacs-config helper with live reads, safe bounds, and a read-only inspector. OpenRouter and ChatGPT Codex load private bounded catalog caches before interaction, refresh asynchronously after five idle seconds, and complete model selection from their discovered provider-order lists. Codex probes the configured candidate policy without automatic browser login and accepts HTTP 200/429. Perplexity retains final citations; Copilot device login, Codex PKCE login/refresh, and Grok CLI-state refresh keep provider secrets and bodies in curl stdin rather than argv. `quick-lookup` remains tool-free; `project-readonly`, `codex-agentic`, and `grok-build-oauth-agentic` run bounded local tool loops; conditional `web-readonly`/`github-readonly` add their configured stdio MCP server. Named presets retain local and MCP tool policy. Private preset persistence and Claude/ChatGPT handoff are ported. Native-CLI response regeneration, media, graphical ediff and multi-region/diff-option rewrite controls, model discovery for providers other than OpenRouter and ChatGPT Codex, parallel replies within one buffer, and Copilot Responses/media/tool support remain open (`src/llm-conversation.lisp`, `src/llm-context.lisp`, `src/llm.lisp`, `src/llm-request-ui.lisp`, `src/llm-rewrite.lisp`, `src/llm-response-history.lisp`, `src/llm-claude-sessions.lisp`, `src/apps/llm-presets.lisp`, `src/llm-visuals.lisp`, `src/llm-trace.lisp`, `src/llm-models.lisp`, `src/llm-codex-models.lisp`, `src/startup.lisp`, `scripts/llm-conversation-test.sh`, `scripts/llm-backend-test.sh`, `scripts/llm-workflow-test.sh`, `scripts/llm-models-test.sh`, `scripts/llm-codex-models-test.sh`). |
| gptel-claude-code / gptel-codex / gptel-grok-build | ported/partial | The three native process backends render bounded backend-specific agent events and resume validated buffer-local sessions. Claude launches at the originating canonical Git root with the configured nine-tool allowlist and safe project-then-home MCP config discovery; inherited Org `CC_CWD` and nearest `CC_ALLOWED_TOOLS` properties override cwd/session scope and tools exactly. Its thinking/tool/tool-result activity uses the configured semantic `cc_*` Org blocks; completed thinking and result blocks auto-collapse, tool blocks collapse only above eight lines, and conversation-local `C-c C-t` toggles all results without mutating transcript bytes. Claude result metadata is attached to Assistant spans; `C-c C-f` safely forks the project JSONL history at that UUID, `C-c C-b` selects the indexed session, and an earlier send before a same-session continuation performs the fork automatically. Wider backend-specific controls remain absent (`src/apps/llm-cli.lisp`, `src/llm-claude-blocks.lisp`, `src/llm-claude-sessions.lisp`, `scripts/llm-backend-test.sh`). |
| gptel-chatgpt-codex / gptel-grok-build-oauth | ported/partial | Separate `chatgpt-codex` and `grok-oauth` backends share the official CLI auth files, renew credentials, retain per-buffer histories, and run the configured five-tool agentic presets. Codex includes a real loopback PKCE command, Responses SSE/tool translation, stable session/cache IDs, one 401 retry, and the configured cached asynchronous candidate probing with compatible preset fallback; Grok uses exact CLI-proxy headers and `grok models` refresh. Sandboxed native gates cover both transports, Codex model discovery, the missing-auth/no-browser rule, and the complete credential/argv boundary (`src/apps/llm-oauth.lisp`, `src/llm-codex-models.lisp`, `scripts/llm-oauth-test.sh`, `scripts/llm-codex-models-test.sh`). Live ChatGPT/xAI acceptance on ex44 remains external. |
| gptel-tooling / gptel-stability | ported/partial | The five configured local read-only tools are exposed by opt-in OpenRouter, native ChatGPT Codex, and Grok OAuth presets, with captured project roots, bounded direct-argv ripgrep, canonical UTF-8 file reads, Lem/Common Lisp symbol help, fragmented chat-completions and Responses call assembly, bounded rounds, visible results, cancellation, and killed-buffer process/marker cleanup (`src/llm-tools.lisp`, `src/llm.lisp`, `src/apps/llm-oauth.lisp`, `scripts/llm-tools-test.sh`, `scripts/llm-oauth-test.sh`, `scripts/llm-conversation-test.sh`). `web-readonly` and `github-readonly` add persistent bounded stdio MCP sessions, paginated namespaced schemas, visible text/structured/resource results, fixed read-only GitHub toolsets, token-free argv, and abort cleanup (`src/llm-mcp.lisp`, `scripts/llm-mcp-test.sh`). Perplexity and Copilot remain text-chat-only; `read_emacs_symbol` necessarily translates to Lem's symbol space; arbitrary third-party MCP definitions and advanced client capabilities are not ported. |
| claude-code.el | lem-builtin | `lem-claude-code` extension, `C-c c` |
| monet | ported/partial | The project-aware `C-c c` Claude session starts an authenticated loopback MCP server, passes Claude a private mode-0600 HTTP config plus an inspection/review-only allowlist, and exposes `openDiff`/`checkDiff`. A focused read-only unified diff accepts with `y` as one undoable live-buffer transaction or rejects with `q`; stale proposals fail closed. The patched server hides arbitrary Lisp evaluation and command execution and disables unrestricted `file://` resources. This uses Streamable HTTP and whole-buffer unified review rather than Monet's authenticated websocket lockfile, IDE notifications, deferred response, and Ediff UI (`src/apps/claude-bridge.lisp`, `patches/lem-mcp-server-secure.patch`, `scripts/claude-bridge-test.sh`). |
| mcp.el | ported/partial | Lem remains an authenticated MCP server for Claude, and Lem-yath now also acts as the configured stdio client hub for fetch and read-only GitHub. M-x commands connect one/all, report status, and stop all; fixed-server policy replaces mcp.el's arbitrary server registry and rich hub UI. |
| notmuch | ported/partial | `M-x lem-yath-notmuch` provides a focused, read-only newest-first list and nested plain-text thread view. Real bare search-result IDs become exact `thread:` queries, opening visible mail removes `unread`, and the configured Evil-collection surface is retained: search triage `a`, `d`, `!`, `=`, `+`, `-`; show message `a`, `x`, `d`, `=`, `+`, `-`; show thread `A`, `X`; new mail `C`/`cc`; sender/all reply `cr`/`cR`; forward `cf`; shown-draft resume `e`; received-part save `. s`. Tag/archive mutations use bounded direct argv and success-only projection changes. New mail uses the Notmuch identity; replies use exact CLI templates; ordinary forwarding retains the stock `[Sender]` subject, References, included headers, delimiters, and regular attachment bytes. A protected source instead becomes one private, byte-exact `forwarded-message.eml` attachment so signature/encryption bytes are not invalidated. `To`/`Cc`/`Bcc` gain Corfu-style automatic completion from recipients in mail sent by the configured Notmuch identities: a three-character query runs asynchronously through bounded direct argv, superseded work is cancelled, longer input refilters a per-compose cache, and acceptance replaces only the current comma-separated token. `C-c C-a` prompts for a regular local file and inserts an Emacs-shaped MML marker. `C-x C-s` snapshots a MIME draft through `notmuch insert --create-folder --folder=drafts +draft`; `C-c C-p` saves and closes; `e` restores editable text, reply/forward state, and attachment bytes into owner-private temporary files. Replacement hides the older draft only after insertion, cancel retains the saved version, successful send retires it after SMTP/FCC, and cleanup removes extracted files. `C-c C-c` safely expands up to 16 markers into a 7 MiB-bounded `multipart/mixed` message, performs credential-private local-Bridge STARTTLS submission, handles Bcc at the envelope, writes the exact normalized Notmuch `sent` FCC, and applies success-only `+replied`/`+forwarded`; stage-aware retry cannot duplicate SMTP after an FCC/tag failure. `C-c C-k` cancels. Show buffers retain the bare Message-ID for `C-c s e`; received MIME leaves render as selectable rows. Return previews PDFs in owner-private ephemeral storage and saves other parts; `. s` saves any part with a safe proposed basename, remembered directory, overwrite confirmation, mode-0600 same-directory staging, atomic rename, and symlink/non-regular refusal. Stateful real-ncurses coverage proves physical triage/compose/reply/ordinary-and-protected-forward/send/completion/attachment/draft/received-part keys, real Notmuch address-query grammar, a real local STARTTLS/AUTH exchange, binary MIME and ordinary/protected-forward/draft/received-part fidelity, malformed-address and unsafe-attachment refusal, exact query/argv/template/FCC scope, duplicate-safe injected failure recovery, navigation, refresh, Salta routing, private resume/received-PDF cleanup, received-save overwrite/refusal semantics, and `mbsync -a && notmuch new` (`src/apps/notmuch.lisp`, `src/apps/documents.lisp`, `src/apps/salta.lisp`, `scripts/lem-yath-smtp-submit.py`, `scripts/notmuch-test.sh`). The rest of stock Notmuch's richer part-action map and Outlook integration remain absent. |
| elfeed + elfeed-protocol | ported/partial | `M-x lem-yath-elfeed` reads the configured Miniflux Fever endpoint into focused, read-only Vi-priority list and article panes. Refresh preserves the selected item, generation ownership rejects stale asynchronous responses, empty and failed requests render distinctly, HTML is reduced to readable text, `b` opens the source, and show-mode `A` uses the configured `archive.is` URL. Credentials come from authinfo, the derived API key stays on bounded curl stdin rather than argv, and a stateful network-free ncurses gate covers the path (`src/apps/elfeed.lisp`, `scripts/elfeed-test.sh`). Full Elfeed filtering/local-database behavior and an in-editor EWW equivalent remain absent. |
| devdocs | ported/partial | `SPC h d` asynchronously fetches or reuses a session-cached index, prompts with Prescient filtering, and opens sanitized documentation in a focused read-only pane with Vi-priority `b`/`q`. `M-x lem-yath-devdocs-install` now validates and caches an index before registering its slug; bounded direct-argv requests, exact fragment-bearing browser URLs, failed requests, and latest-generation ownership for out-of-order index/page replies are covered by a network-free ncurses gate (`src/apps/devdocs.lisp`, `scripts/devdocs-test.sh`). Unlike Emacs DevDocs, installed indexes are not persisted across Lem sessions. |
| pdf-tools | ported/partial | Ordinary `.pdf` files open as a read-only, non-file buffer that extracts one bounded page at a time with Poppler. `n`/`p`, `PageDown`/`PageUp`, `g`, `G`, `r`, `q`, and external-viewer `o` cover the reading path, and the same mode previews Notmuch PDF parts. Pixel layout, images, links, annotations, forms, and other visual semantics remain with the external viewer (`src/apps/documents.lisp`, `scripts/documents-test.sh`, `scripts/notmuch-test.sh`). |
| nov (EPUB) | ported/partial | Ordinary `.epub` files are converted through bounded sandboxed Pandoc into a read-only Markdown buffer with `n`/`p`, `PageDown`/`PageUp`, `g`, `r`, `q`, and external-viewer `o`. The view deliberately omits Nov's HTML/CSS layout, images, and wider command surface (`src/apps/documents.lisp`, `scripts/documents-test.sh`). |
| vterm | lem-builtin+ported/partial | `lem-terminal` (libvterm), exposed as both `M-x vterm` and `M-x terminal`. `src/terminal.lisp` supplies the effective Evil Collection state flow: initial Insert, Escape/C-x `[` to a live Normal/copy view, `i/I/a/A` back to raw input, Normal `p/P` and Return, and `C-c C-z` Escape routing. The Nix-only native patch uses direct literal-directory spawning and reaps the child during buffer cleanup (`patches/lem-terminal-safe-cwd.patch`, `scripts/terminal-test.sh`). Prompt-mutating Normal operators such as delete/change remain absent because Lem has no safe child-prompt edit API. |
| pgmacs / pg | ported/partial | `M-x pgmacs` accepts a password-free libpq connection string (or standard PG* defaults), lists ordinary tables, and opens a focused read-only psql-backed result view. `M-x lem-yath-pg-query` renders CSV results, while Vi-priority `g` refreshes the captured query and `q` returns to the source buffer. Calls use bounded direct argv, reject embedded connection-string passwords in favor of `.pgpass`/PG* credentials, and are exercised against a private real PostgreSQL server (`src/apps/pg.lisp`, `scripts/pg-test.sh`). PGmacs' editable rows, pagination, rich object/procedure browser, metadata, exports, and widgets remain absent. |
| salta.el | ported/partial | All six configured `C-c s` property, reckoner, contractor-rate, financial, and payment workflows use bounded asynchronous PostgREST requests, focused read-only Vi-priority list/detail panes, visible-value copying, captured refreshes, and latest-request ownership. The service-role key, request URL, and JSON body stay in curl's stdin config rather than argv; a fake-API installed-wrapper gate covers exact request shapes, contractor-cache reuse, detail `w/r/c/p/g`, totals/margin, unprefixed payment behavior, source preservation, and stale responses (`src/apps/salta.lisp`, `scripts/salta-test.sh`). The configured Notmuch `C-c s e` bridge tracks the message at point and opens its exact encoded payment-email URL through a direct desktop argv (`src/apps/notmuch.lisp`, `scripts/notmuch-test.sh`). Validation against the owner-operated live API and web application remains external. |
| helpful | ported/partial | `SPC h k` and `SPC h v` retain package-qualified typed selection, then open a shared read-only Helpful mode with documentation, exact definition source, and bounded SBCL caller/reference rows. Exact `M-x describe-face` uses the same mode for the live attribute's current theme layer, effective properties, genuinely styled pangram, and defining form. `n`/`p` and `Tab`/backtab traverse rows cyclically, Return visits one, `s` visits the definition, `g` refreshes stale locations, and `q` restores the originating window. `SPC h K` resolves a physical key into the same command-help buffer; `SPC h b` retains Lem's core binding list. The ncurses gate covers secret-safe values, same-name packages, face selection/styling, forward/reverse navigation, stale-source refusal and recovery, exact form offsets, key inspection, and reload (`src/help.lisp`, `scripts/help-test.sh`). Helpful's advice, disassembly, debugging/customization sections, syntax-highlighted embedded source, and semantic button graph remain absent. |
| which-key | ported/partial | global guidance snapshots every active ordinary keymap-backed prefix across global, dynamic mode, and Vi-state maps, using live dispatcher lookup to preserve shadowing. Initial and nested pages each wait a fresh second, then render sorted raw command/`+prefix` labels in width-bounded cyclic columns capped at 25% of frame height. The pinned `C-h` dispatcher implements `n/p`, nested `u`, docstring `d`, focused `h`, abort `a`, digit arguments, and pre-popup help without mutating the live maps; physical narrow-terminal paging, dispatch, reload, stale timers, and native-transient isolation are covered in `scripts/ui-parity-test.sh`. Exact separators/default replacements and echo-area styling remain different; undoing a one-key prefix opens Lem's focused top-level binding view instead of retaining a root Which-Key popup. |
| transient | lem-builtin | `lem/transient`; native transient menus remain isolated from global prefix guidance, opening after the upstream 500ms delay and refreshing nested menus immediately (`scripts/ui-parity-test.sh`) |
| multiple-cursors | lem-builtin | core multi-cursors (`M-C`, isearch add-cursor); Emacs config only used it internally |
| expreg | ported/partial | repeated `SPC v` expands lexical subword → symbol → syntax nodes from all 24 packaged tree-sitter modes, with whitespace-sensitive balanced-list interiors and enclosing-pair tiers both in code and inside parser-bounded ordinary/block strings, plus a paragraph fallback in parserless modes (`src/vi.lisp`, `scripts/expreg-test.sh`, `scripts/tree-sitter-test.sh`). The installed grammar selected for highlighting is also the grammar used for expansion, including distinct C++, extension-sensitive TSX, GDScript, Just, Nu, and Typst. Arbitrary forward and reverse Visual selections generate from their active endpoint, skip contained candidates into contraction history, and never invent the arbitrary range as a tier. `M-x expreg-contract`, declared but unbound in the Emacs configuration, walks backward through the generated buffer-local sequence; `SPC v` then moves forward again. Parser results and contraction state are valid only for the unchanged buffer. Malformed error fragments use useful containing nodes rather than Expreg's partial error ranges. |
| vundo | ported/partial | `SPC u` opens a three-row Unicode retained tree with historical source-point restoration, live preview, counted branch/sibling/saved-node navigation, stem navigation, mark/diff, save, rollback, and accept (`src/vundo.lisp`, `patches/lem-undo-tree.patch`, `patches/lem-undo-state-point.patch`, `scripts/vundo-test.sh`); internal debug keys `i`/`D` are absent |
| pulsar | ported | `src/jump-feedback.lisp` reproduces the configured `pulsar-delay=0.03`, four fading TTY-red iterations, centered accepted Consult destinations, and narrow activation policy for `SPC SPC`, `C-c i`, and `SPC p s`. Preview focus and cancellation do not pulse, and the final stage removes its timer and overlay. Generic `M-x imenu` follows its distinct configured hook by recentering without a pulse (`src/imenu.lisp`, `scripts/project-outline-test.sh`, `scripts/project-navigation-test.sh`, `scripts/lsp-project-test.sh`). |
| indent-bars | ported/partial | programming buffers render display-only, depth-colored `│` guides at mode/EditorConfig indentation levels, including contextual blank lines and tab-expanded columns; multiline strings retain only their opening-context depth. `M-x lem-yath-toggle-indent-guides` is buffer-local (`src/indent-guides.lisp`, `patches/lem-display-line-transformer.patch`, `scripts/indent-guides-test.sh`). Ncurses characters and the native Modus projection approximate Emacs' pixel stipple, face blending, and GUI behavior. |
| rainbow-delimiters | ported/partial | `src/ui.lisp` colors every syntax-table delimiter pair by depth in programming buffers, excluding strings, comments, and escaped delimiters. All nine default depths plus distinct unmatched and mismatched faces use the active Modus palette, and show-paren remains active. Individual Lem syntax tables can still classify language constructs differently from Emacs (`scripts/ui-parity-test.sh`). |
| display-line-numbers (built-in) | ported | relative numbers render in saved and unsaved programming buffers, compose with other gutters, and stay out of prose and utility buffers (`src/ui.lisp`, `scripts/ui-parity-test.sh`) |
| centered-view-mode (custom) | ported | `SPC y c` toggles a buffer-local `Center` mode at configurable width 100, with a buffer-local width override used by the business document profile. A narrow pinned display API supplies balanced per-window margins to drawing, wrapping, horizontal scrolling, and screen-line geometry; resize, split, narrow-window, reload, continuation-row, and restoration behavior are covered by `scripts/centered-view-test.sh` and `scripts/business-visual-test.sh` (`src/centered-view.lisp`, `patches/lem-centered-content-width.patch`). |
| business-visual / business-document modes (custom) | ported/partial | `src/business-visual.lisp` auto-enables the alternate light office profile only on configured host `workwin`; `M-x business-visual-mode` permits an explicit trial elsewhere. Org, Markdown/EPUB, plain text, Notmuch messages, feed entries, and DevDocs gain an 88-column centered/wrapped page, fill width 88, a compact modeline, calm semantic palette, shape-only cursors, and disabled jump pulses. Toggle-off restores the exact prior theme, modeline, pulse setting, and buffer presentation (`scripts/business-visual-test.sh`). Ncurses cannot implement proportional fonts, fractional line spacing, hollow cursors, fringes, GUI chrome, or modes Lem does not have. |
| truncate-lines / visual-line-mode / hl-line-mode (built-ins) | ported | `src/ui.lisp` starts with long lines truncated and disables Lem's upstream current-line highlight, matching the active Emacs baseline; `SPC y v` retains buffer-local wrap toggling, activates the configured modal row policy, and prefers shared space/tab word boundaries with exact-width fallback for long tokens (`patches/lem-word-boundary-wrap.patch`, `scripts/ui-parity-test.sh`, `scripts/screen-line-test.sh`). |
| tab-bar / winner-mode (built-ins) | ported/partial | no tab header is shown at startup; `C-x t 2` lazily enables Lem's frame multiplexer and creates a tab. `C-c Left` / `C-c Right` traverse a bounded, frame-local Winner-style history of ordinary split topology, proportions, buffers, selection, and view positions while preserving live buffer points (`src/window-history.lisp`, `scripts/window-history-test.sh`). Lem's tabs remain frame-multiplexer approximations, and transient/side/attached windows are deliberately outside the history. |
| dirvish | ported/partial | Ordinary directory visits use Lem's full-buffer `directory-mode` with the pinned hidden-details presentation: compact names and one display-only, right-aligned six-cell file-size/direct-child-count field (`src/dirvish.lisp`, `patches/lem-directory-buffer-clean.patch`, `scripts/dirvish-test.sh`). The styled path row remains visible, its redundant blank successor is hidden, and the footer shows pinned ascending `name|mtime`, selected-symlink target, and current-entry/total segments. Plain `M-x dirvish` uses the current directory and opens the pinned one-parent/current/preview full-frame proportions with the same chrome; a prefix prompts elsewhere. Selection drives a debounced/throttled raw preview: regular UTF-8 text is mode-hook-free and capped at 1 MiB, directories at 200 direct entries, and binary, oversized, undecodable, symlink, pipe, socket, or device entries remain unopened and show metadata. Cancellable background direct argv adds terminal-safe dispatch: libarchive lists at most 200 archive members without extraction, Poppler renders the first PDF page, sandboxed Pandoc converts bounded EPUB text, and `file(1)` reports image/media metadata. External previews have a three-second timeout, 512-KiB output cap, 128-MiB document/archive input cap, and current-selection ownership. File `Return` and `q` restore the exact prior ordinary-window layout, while `dirvish-layout-toggle` retains the directory after restoration. Native exact-path operations remain intact, terminal resize recomputes alignment, and Dired-style five-second external refresh preserves the selected entry, cursor column, and surviving marks (`src/persistence.lisp`, `scripts/persistence-test.sh`). Lem lacks a geometry-safe per-window header line, so the path row scrolls instead of staying sticky across the directory panes. Pixel image/video rendering, subtree/collapse extensions, and wider integrations remain absent; Lem's Filer is a separate side tree. |
| find-name-dired (built-in) | ported/partial | `M-s f` asynchronously fills a persistent, read-only `*Find*` buffer with safely escaped exact-path rows (`src/find-name.lisp`). It matches the pinned Dirvish default presentation with a display-only, right-aligned six-cell size field (direct child count for directories), including resize-safe alignment. Dired `m`/`u`/`U`/`t`, `C`/`R`/`D`, marked-or-current targeting, overwrite and top-level recursive copy/delete confirmation, refresh retention, and request-local cancellation are covered in the real TUI; the wider Dired operation surface remains a gap. |
| electric-pair-mode / delete-selection-mode (built-ins) | ported/partial | syntax-table delimiter/quote pairing plus Unicode smart quotes, full forward preserve-balance scanning, numeric prefixes, ordinary region replacement, Emacs-style opener/quote region wrapping, and preflighted adjacent-pair Backspace in Emacs or Vi insert editing; pair deletion preserves completion, prompt, snippet, Paredit, read-only, signed-prefix kill-ring, point-correct undo, and Vi-state lifecycles. Forward scanning crosses balanced forms, stops at the first genuine mismatch, and isolates strings/comments. An unmatched embedded quote is escaped to keep the Lisp string valid instead of reproducing Lispy's raw interior quote, while Emacs's destructive wide-selection quirk remains an intentional gap (`src/electric-pair.lisp`, `scripts/electric-editing-test.sh`) |
| ws-butler | ported | track changed programming-buffer lines and trim only those lines on save (`src/editing.lisp`); EditorConfig `trim_trailing_whitespace=true` additionally normalizes the whole buffer, while false/absent retains touched-line cleanup |
| ibuffer | ported/partial | `C-x C-b` keeps Lem's native marked multi-column chooser but partitions its recency-ordered snapshot with the configured exclusive first-match org/tramp/emacs/ediff/dired/terminal/help groups, omits empty groups, and places unmatched buffers under `Default`. Its default rows carry mark, modified/read-only status and live `L` lock state, 18-cell elided names, 9-cell right-aligned sizes, 16-cell elided modes, and filenames. Each group has an Ibuffer-style `[ name ]` heading; Return collapses it to `[ name ... ]` or expands it, and heading rows cannot be marked, saved, or killed. The effective Evil-Collection `o a/v/s/f/m`, `o i`, comma, and backtick controls provide all stock name/recency/size/file/mode sorts, reversal, lexical cycling, and both configured formats without losing groups or marks. `s m/n/f/b/.` provide live case-insensitive regexp filters for used mode, name, full filename, basename, or extension; Return pushes the pending filter and Escape cancels it. `s RET` selects comma-separated exact registered modes, `s M` selects snapshot modes or CLOS parents, `s *` matches GNU-style starred names, `s </>` apply strict character-size bounds, and `s c` matches content case-insensitively while skipping buffers above 16 million characters, `s E` retains live generic, shell, compilation, and terminal process owners, and `s F` matches a file's containing directory or a non-file buffer's working directory. `s i/v` push modified and visiting-file filters onto an AND stack, and `s !`, `s p`, and `s /` negate, pop, or disable it. The everyday modal core includes `m/u/Backspace/U/t/~` ordinary marks; visible special/modified/unsaved/read-only/directory/dissociated/help/compressed marking on `* *`/`* s`, `* m`, `* u`, `* r`, `* /`, `* e`, `* h`, and `* z`; exact used-mode marking on `* M`; name/mode/file/bounded-content regexp marking on `% n/m/f/g` with atomic invalid-regexp refusal; GNU `.` age marking from real last-window-display timestamps, a configurable 72-hour default, strict threshold comparison, and never-displayed exclusion; session-local `-`/`+` temporary hide/force-show regexps with `gR` deferral, `gr` activation, and force-show precedence; `K` temporary ordinary-mark row hiding through `gR`, with unmarked restoration on `gr` and unrelated deletion-mark preservation; cyclic `{`/`}` ordinary-mark navigation; distinct `d` deletion marks plus `x` execution; `S` marked save; GNU default-`all` locking on `L` and locked-row marking on `% L`, with buffer-kill and editor-exit refusal before teardown; marked `M/T/R` modified/read-only/Emacs-style unique-name changes; one-confirmation `V` reversion of ordinary marks or the implicitly marked current row, with deletion-mark exclusion and per-buffer failure isolation; focused `X` burying; `gj/gk` row movement; group jumps on Tab/backtab, `C-j/C-k`, and `]]/[[`; exact group completion on `M-j`; and `q`. `gR/gr` redisplay or rebuild the snapshot with state preservation, `yb/yf` copy exact names; `go` selects the focused buffer in another ordinary window; `C-o` displays it there without leaving the chooser; `M-o` visits it in a sole ordinary window; `A`/`gv` stack ordinary-marked buffers and `gV` places them side by side, with `D` exclusion and current-row fallback; and `J`/`M-g` complete over the exact snapshot, reveal collapsed target groups, and respect active filters. `=` opens a focused, read-only, bounded unified diff for ordinary-marked file buffers or the unmarked current row, excluding `D` and non-file buffers and preserving the prior view on a missing-file failure. `O` and `M-s a C-o` provide bounded persistent Occur over ordinary marks with GNU reverse ordering, `D` exclusion, implicit-current marking, smart case, multiline/context rendering, live targets, and selected/no-select modal navigation. `M-s a C-s` and `M-s a M-C-s` provide literal and regexp incremental search over explicit ordinary marks in display order, with initial-buffer pause, cross-buffer repeat/wrap, same-kind history, and complete finish/abort mode cleanup. `Q` and `I` provide display-order smart-case literal/regexp query-replace over ordinary marks or the implicit current row, with `D` exclusion, per-buffer `y/n/!/q/.` confirmation, GNU case-pattern transfer plus `\&`, `\1`–`\9`, `\\`, and `\#` expansion, restored picker state, one undo unit per affected target, and fail-closed read-only/regexp/replacement validation. Control-character-safe labels and reload are retained (`src/buffer-list.lisp`, `scripts/buffer-list-test.sh`). Multi-buffer `V` omits GNU Ibuffer's auxiliary confirmation-name window; the bounded diff view uses concise buffer headings instead of GNU Emacs's shell-command transcript; Ibuffer's predicate filters, compound/saved filter operations, and other-frame, view-and-eval, Occur edit/rename/clone, shell, eval, and print bulk operations remain absent; regexp query-replace omits GNU Lisp-evaluated `\,`, per-match `\?` editing, zero-width matching, and advanced interactive responses; mode completion uses package-qualified labels, and CL-PPCRE regexp syntax can differ from Emacs regexp syntax. |
| bookmarks (built-in) | ported | `lem-bookmark` retains `SPC b m` / `SPC RET`; its jump prompt adds display-only target type, abbreviated path, exact line/column, and bounded line context without opening unvisited files, while stale targets degrade to missing-path metadata. `src/bookmarks.lisp` restores bookmarks at startup and saves them through the private atomic persistence transaction at clean exit. A three-way merge preserves independent additions and respects local updates/deletions across stale editor processes (`scripts/bookmark-test.sh`). |
| avy | ported/partial | `SPC l/a/s` provide visible line, character, and symbol-start selection with balanced `a/s/d/f/g/h/j/k/l` floating labels, normal all-window and Visual current-window scope, closest case-folded character ordering, punctuation-aware symbols, wrapped/hidden-row handling, resize invalidation, raw-digit line fallback, cancellation, jumplist integration, and source non-mutation (`src/avy.lisp`, `scripts/avy-test.sh`). All default `x/X/t/m/n/y/Y/i/z/?` dispatch/help paths are present. `i` uses flake-packaged `en_US` Aspell for target-word or selected-line correction, with Prescient-filtered numbered suggestions, `Space` keep-once, `a` process-local acceptance, `i` personal-dictionary persistence shared with Emacs, `r` manual replacement, origin/state preservation, and one-step undo. Buffer-local dictionaries, dictionary switching, Flyspell UI, non-alphabetic Ispell word syntax, exotic display/syntax geometry, and exact Emacs choices-window presentation remain gaps. |
| gcmh / no-littering / use-package / direnv / sops | n/a or ported/partial | SBCL needs no Emacs GC hack and no-littering/use-package do not map directly. Direnv is isolated in `src/direnv.lisp`: the current eligible buffer selects Lem's global process environment, explicit `direnv-allow` is available without auto-authorization, and `PATH` affects future subprocesses (`scripts/direnv-test.sh`). `src/sops.lisp` transparently decrypts existing local SOPS text files, installs an encrypting save writer, inhibits format-on-save, and fails closed on decryption errors (`scripts/sops-test.sh`); guided new-file creation and input-type overrides remain absent. |
| editorconfig | ported/partial | the official CLI resolves hierarchy/inheritance for every steady-state local file buffer; Lem maps indentation, line endings, write charset, fill column, trailing whitespace, and final-newline policy (`src/editorconfig.lisp`, `scripts/formatting-test.sh`). Charset is applied only to subsequent writes, not initial decoding |
| auto-revert / savehist / save-place / recentf (built-ins) | ported/partial | `src/file-notify.lisp` and `src/persistence.lisp` use bounded Linux inotify parent-directory watches for prompt clean local-file refresh, including same-metadata rewrites and delete/recreate replacement. Watches are shared, survive ordinary atomic replacement, and are released on buffer kill, Save As, configuration reload, and editor exit. Matching the configured Emacs default, a reload-owned regular five-second safety scan still covers every buffer, with pre-command and selection fallbacks; symlinks, unsupported or failed watches, and local directory/non-file adapters rely on that scan exclusively. Reloads remain transactional and limited to clean readable files up to a 64 MiB safety cap; stale saves are protected, directory listings refresh without losing selection, column, or marks, and up to 600 local-file positions or exact selected directory entries are restored. Bookmarks, allowlisted non-secret prompt histories, a 120-entry live/40-entry saved Vi-aware kill ring, and separate 16-entry literal/regexp search rings persist atomically with baseline-aware three-way merging; recentf remains a 300-file MRU on `M-g r`. Larger automatic file reloads and adapters for other non-file buffers remain gaps (`scripts/persistence-test.sh`) |
| modus-vivendi-tinted (built-in) / doom-themes | ported/partial | the active Emacs startup theme is recreated natively in `src/theme.lisp`; resolved semantic colors are tested, while ncurses rendering is limited by the terminal color model and Lem has fewer face roles. `doom-themes` remains declared but inactive. |
| notmuch-outlook | gap | the WSL-specific Outlook integration remains external |
| nodes-org-sync | ported/partial | `src/org/nodes-sync.lisp` reproduces the configured default-`nova`/environment host allowlist, canonical `$WORKDIR` containment, Syncthing-conflict refusal, buffer-local before/after-save lifecycle, default-off actionable ID creation with explicit manual promotion, and exact asynchronous `nodes-org-sync --quiet --file FILE` argv. The installed-wrapper gate covers reload, physical saves, metacharacters, source-block decoys, symlink escapes, and failure-buffer behavior (`scripts/org-nodes-sync-test.sh`). The live projector/PostgreSQL acceptance on `nova` remains external, and Lem adds a five-minute/1-MiB subprocess bound. |

## Behavioral divergences worth knowing

- **Display palette and delimiters**: Lem retains the configured Modus hex
  values for its available semantic attributes, but ncurses maps them through
  the terminal's color capabilities. Nested colors cover six cycling depths in
  Common Lisp only; other modes retain matching-pair highlighting rather than
  Emacs's all-`prog-mode` rainbow coverage.
- **Tabs and layout history**: startup correctly has no tab row and `C-x t 2`
  creates one on demand. The frame multiplexer remains an approximation of
  Emacs tabs, but each frame now has independent, bounded Winner-style ordinary
  window history on `C-c Left` / `C-c Right`. Floating, prompt, side, header,
  and attached windows retain subsystem ownership rather than entering history;
  configurations containing dead or boring buffers are skipped as a unit.
- **Surround grammar**: standard `ys`/`ds`/`cs` and visual `S` work, including
  row-wise Visual Block edits, common padded delimiters, tags, call forms, and
  syntax-context-aware balanced character pairs. Mode-local custom pairs, full
  minibuffer editing, and multi-character delimiter editing remain absent;
  Python triple-quote edits fail closed.
- **Formatting lifecycle**: mapped programming modes with an available,
  successful backend format synchronously before the ordinary save, so the
  formatted state reaches disk through that single write before LSP `didSave`.
  External commands use stdin and direct argument vectors under a timeout. CLI
  launch, timeout, nonzero-exit, and output-limit failures occur before buffer
  mutation, contribute no formatter output, and do not fall back to LSP;
  transactional ws-butler and EditorConfig normalization still runs. Successful
  diff application and save normalization share a retained change group;
  formatter hunk read-only ranges are preflighted, while mutation-hook or
  normalization errors
  replay coherent inverse notifications and restore exact buffer/history state.
  A safely missed touched-line cleanup remains pending for a later save.
  If cancellation itself becomes unsafe, Lem fails visibly dirty with truncated
  history and aborts the ordinary save. Safe formatter failures continue that
  one save after retrying save normalization without formatter output. LSP
  fallback is manual-only when no mapped
  backend is usable. There is no async worker, formatter prompt, or
  Apheleia-style per-project backend table.
- **EditorConfig scope**: matching is delegated to the official CLI, but Lem
  maps only the documented core properties. A charset rule cannot re-decode an
  already opened file and affects later writes only; UTF-16BE/LE writes do not
  add a BOM. `trim_trailing_whitespace=false` does not disable ws-butler's
  touched-line policy. Direnv is a separate module and can change executable
  lookup for later formatting runs through the process-wide `PATH`.
- **Direnv process scope**: `src/direnv.lisp` follows the exact directory of the
  current eligible buffer rather than the Git/project root, while
  `src/workspace.lisp` keeps the notes `$WORKDIR` fixed at startup. An
  unwind-protected `execute-find-file :around` provisionally supplies the
  destination environment to initial mode hooks for selected opens, then the
  switch hook makes it current; direct background `find-file-buffer` loads do
  not retarget the editor. Derived process modes and explicitly marked
  process/compilation buffers participate, while arbitrary scratch buffers do
  not. Updates are synchronous, bounded by a 300-second safety timeout and
  streaming 4 MiB limits per output stream, and affect Lem's global SBCL
  environment and future subprocesses only. Prevalidated changes and any
  rollback are applied sequentially, not atomically: existing processes keep
  their launch environment, and worker threads may observe an intermediate
  multi-variable state or whichever buffer environment is active when they
  launch. `scripts/direnv-test.sh` supplies the focused ncurses evidence.
- **SOPS file scope**: `src/sops.lisp` recognizes existing local YAML, JSON,
  dotenv, INI, and text files through `sops filestatus`. Successful opens keep
  plaintext only in the live buffer; the patched buffer-local writer sends it
  to `sops encrypt --filename-override` and writes only returned ciphertext.
  Failed decrypts remain read-only and retry through revert; failed encryption
  leaves both the prior disk ciphertext and modified plaintext buffer intact.
  The integration is synchronous, has no guided creation command or custom
  input-type map, and uses persistence polling rather than file notifications.
- **Org editing scope**: `.org` files use the native lem-yath Org mode. Its
  TUI-tested boundary covers semantic faces, non-destructive local/global
  folding, atomic hidden-line motion and reveal, safe heading insertion, the
  complete configured TODO cycle with immediate saving, reload/multi-buffer
  cleanup, checklist continuation/toggling, relative file links, table
  row/cell targeting and alignment, and complete-subtree transforms. It is not
  GNU Org: Evil-Org themes beyond the bounded active text objects and the
  wider operator/endpoint themes, prefixed live
  source-block sessions, richer list/table transforms, and exact graphical
  org-modern/agenda presentation remain absent. The active terminal-safe
  org-modern projection, configured org-download commands, and empty initial
  Org scratch are ported and tested.
- **Completion previews**: the grouped project picker and directory-local
  Elisp outline provide reversible Consult-style preview. Generic command,
  file, buffer, and LSP-symbol prompts still do not preview their targets.
- **Undo accounting**: the configured 2,080,000 / 3,120,000 / 48,000,000
  Vundo budgets are applied to copied UTF-8 edit payload, not Emacs heap usage.
  Lem additionally caps retained history at 65,536 nodes, 262,144 edits, and
  128 MiB of UTF-8 route-validation work. Retained states restore their
  historical source point for ordinary undo/redo and Vundo preview. Vundo's
  internal `i`/`D` debug commands remain absent. Speculative rectangle/Copilot-style paths
  do not yet use the constrained retained-undo change-group API, so their
  intermediate transactions remain in history.
- **Embark scope**: completion uses `C-c a` because the ncurses input path cannot
  represent `C-.` distinctly. Characterwise regions and Evil Visual Blocks are
  targets, and repeating `SPC e a` cycles and wraps through the typed targets at
  point, but the dispatcher does not yet provide act-all,
  collect/export/live views, arbitrary Embark action-map composition, or the
  wider embark-consult adapter set.
- **Find-name results** persist after opening a match and are safe for spaces,
  control characters, and shell-looking patterns. They provide Dired-style
  marks plus copy, rename, and confirmed deletion. The compact rows reproduce
  the pinned Dirvish default six-cell size/direct-child-count attribute without
  mutating buffer text; Dired's broader chmod/chown/link/shell/compress command
  surface remains absent.
- **Ordinary directory visits** now share that pinned Dirvish presentation in
  full-buffer `directory-mode`: hidden details, compact names, and a resize-safe
  right-edge size/direct-child-count field. A visible path row, hidden redundant
  blank row, and ascending sort/symlink/index footer reproduce the active pinned
  chrome without changing row identity. Filer remains a separate side tree.
  Dirvish's full-frame layout and bounded terminal preview are ported separately;
  its sticky cross-pane header, subtree layer, and wider integrations are not.
- **Buffer list UI**: `C-x C-b` now has the exact configured first-match group
  partition, empty-group policy, collapsible heading presentation, and stock
  everyday row fields with fixed cell-safe name/mode elision and right-aligned
  sizes. Its pinned sort prefix, reversal, lexical cycle, and both stock formats
  work without changing group order or marks. `s m/n/f/b/.` isolate regexp
  filter input from modal command letters; exact/derived modes, starred names,
  strict size bounds, bounded content, common mode/name/file/extension, and
  modified/visiting-file filters plus stack negation, pop, and disable are
  present alongside the effective everyday and non-prompt starred marks,
  backward-unmark, ordinary-mark navigation, delete, save, state-change,
  unique-name, revert, exact snapshot jump, bounded file diff, bury,
  row-navigation, exact group completion, group-navigation, redisplay/update,
  copy, selected/no-select/one-window visits, balanced stacked or side-by-side
  marked-buffer views with current-row fallback, bounded persistent marked-buffer
  Occur on `O`/`M-s a C-o` with smart case, multiline/context rendering, live
  source targets and modal visit/preview navigation, and quit bindings.
  GNU default-`all` buffer locks are shown as `L`, toggled with `L`, selected
  with `% L`, and refuse both buffer deletion and editor exit before teardown;
  The stock `.` age mark uses last-window-display timestamps, a configurable
  72-hour default, strict comparison, and GNU's never-displayed exclusion.
  `-`/`+` stage session hide/force-show regexps for `gr`, with `gR` deferral and
  force-show precedence; `K` hides ordinary-marked rows until regeneration.
  Ibuffer's remaining predicate and compound/saved filters, Occur
  edit/rename/clone, and other
  specialized bulk operations
  are not reproduced.
  Marked-buffer regexp query-replace omits GNU Lisp-evaluated `\,`, per-match
  `\?` editing, zero-width matching, and advanced interactive responses.
  Content scanning skips buffers above 16 million characters, and mode choices
  use package-qualified labels.
  Multi-buffer revert omits the auxiliary name window, while diff output uses
  concise buffer headings and explicit resource bounds instead of Emacs's
  shell-command transcript.
- **Persistence scope**: clean ordinary local files use prompt bounded Linux
  filesystem notifications alongside the configured five-second safety scan;
  symlinks, unsupported/failed watches, and non-file adapters rely on polling.
  Local directory buffers
  use that cadence and retain the selected entry,
  cursor column, and surviving marks across an external refresh. File positions
  exclude temporary VCS commit files; directory buffers restore the exact
  selected entry on their first fresh-process visit. Shared/unnamed,
  SQL/compile, credential-like, and
  unknown prompt histories deliberately remain memory-only, and other non-file
  auto-revert targets require an explicit buffer-local stale/revert adapter.
  Persistence is default-deny for
  prompt names and applies per-entry and 32 MiB aggregate budgets; oversized
  kills or query strings remain live but are not written. Baseline-aware
  concurrent merging preserves independent additions and intentional removals.
- **Rectangle duplication**: `M-j` matches line and contiguous-region behavior,
  including Vi character/line selections. It also matches the pinned Evil
  V-BLOCK behavior: the block is not an Emacs rectangle or ordinary active
  region, so the cursor line is duplicated while the block and both corners
  remain live. A separate Emacs-style `rectangle-mark-mode` and its
  duplicate-to-the-right behavior remain absent.
- **Electric-pair scope**: syntax-table pairs, Unicode smart quotes, numeric
  prefixes, escapes, syntax-domain-aware forward balance scanning and
  closer reuse/skip, preflighted adjacent-pair Backspace, and recovery after a
  zero-result prompt query are covered. Signed-prefix pair deletion matches
  Emacs's range, directional kill-ring, and undo-point behavior. Emacs's
  orientation-dependent destructive behavior for selections wider than one
  delimiter remains intentionally absent.
- **In-buffer Orderless scope**: automatic mode/Cape completion supports
  multi-component matching and Corfu's `M-Space` separator. CL-PPCRE differs
  from Emacs regexp syntax, and initialism is scoped to deterministic ASCII
  boundaries. `%` directional character folding is covered; upstream `&`
  annotation dispatch is minibuffer-only and has no effective path in this
  configuration's Vertico-Prescient prompts.
- **Snippet scope**: `Tab`, `Shift-Tab`, `C-g`, `C-d`, and `Backspace` provide
  the tested field-session workflow, with completion, Vi, and Paredit retaining
  their intended precedence. Root and table inheritance plus common filename
  mappings expose the pinned community data without executing Emacs Lisp. LSP
  format-2 candidates use the same safe session after tracked final acceptance;
  plain format stays literal, malformed payloads preserve the prefix, and
  backquoted server text is inert. Acceptance-time completion resolve supports
  lazy `additionalTextEdits`; direct and resolved edits remain plain, are
  validated against the primary range and each other, and share one undo step
  with the primary insertion. Resolve failure or a bad additional batch falls
  back to the original primary completion.
  Undo and redo remove live overlays before history replay, then restore the
  selected field, mirrors, and edit hooks when the resulting undo-tree state
  has a matching bounded session snapshot. This covers both expansion replay
  and edits made inside an active field. Stacked
  expansion and direct snippet bindings remain absent because the Emacs profile
  leaves `yas-triggers-in-field` disabled and configures no such bindings.
  Strict TextMate variables/choices/transforms and native transactional
  rollback after arbitrary mutation-hook errors remain gaps. The other LSP
  completion facilities omitted here are also inactive in the configured
  Eglot/Corfu path, as described above. LSP document
  positions consistently use the originating workspace's advertised UTF-16
  encoding, including edits, diagnostics, navigation, symbols, and completion.
  BibTeX expansion deterministically omits automatic indentation; this
  approximates the intended steady-state text rather than reproducing Emacs'
  transient indentation calls.
